# 올댓아라빅 1.4.4 — 예문작성 규칙 (Content Authoring Rules)

> **이 문서의 범위**: 데이터시트에 **무엇을 어떻게 쓸 것인가**(패턴·드릴·레퍼토리·표현·지문·단어의 작성 규칙)만 다룬다.
> 화면 렌더·CSS·레이아웃은 `product-ata144-design.md`, 제품 이론·앱 구조는 `product-ata144.md`, 운영·배포·status는 `CLAUDE.md` 참조.
> 작성자(콘텐츠 입력자)와 검수자를 위한 단일 기준 문서.

---

## 0. 공통 규칙 (모든 타입 공통)

1. **어휘 제약**: **이전 유닛까지 배운 것 + 해당 유닛의 expression·kalimat만** 사용 가능. 그 외(안 배운 어휘) 금지.
2. **자연스러움 — 다음 둘 중 하나여야 한다:**
   - (a) **실제 아랍인들이 자기들끼리 쓰는** 표현/문장, 또는
   - (b) **표준어(MSA)를 써서 외국인을 배려해** 실제로 말할 법한 표현/문장.
   - → 교재에만 존재하는 인위적 문장 금지.
3. **안 배운 패턴 금지.** 단, **해당 유닛에서 새로 도입되는 패턴·kalimat·expression은 사용 가능**.
4. **안 배운 글자 금지.** 그 유닛까지 누적된 알파벳 그룹 안에서만 단어 구성. (예: A02 시점 = A01 `ب ت ث ن ي` + A02 `ا د ذ ر ز و` 글자만)
5. **번역식 배제.** 드릴은 "아랍어→한국어 해석"이 아니라 패턴에 어휘를 바꿔 넣어 **말하기** 연습.
6. **인지 부담 최소.** 설명은 패턴을 이해하는 데 필요한 최소한으로.
7. **새 어휘 등록 원칙.** 드릴·레퍼토리·지문에서 새 단어를 쓰려면 먼저 `kalimat` 또는 `expression`으로 등록할 수 있어야 한다. "좋은 예문이라서 임시로 쓴 단어"가 누적되면 안 된다.
8. **기능 중복 회피.** 같은 유닛 안에서는 같은 기능의 표현·예문을 반복하지 않는다. 형태가 조금 달라도 수업에서 하는 일이 같으면 하나만 남긴다.
9. **예문 다양성 (완전 중복 금지를 넘어 "다양해야 한다").**
   - 패턴 대표문 · 패턴 스크립트 표의 예문 · 레퍼토리 · 드릴은 **서로 다른 동사·주어·목적어로 다양하게** 구성한다.
   - "문장이 완전히 똑같지만 않으면 된다"가 **아니다.** 같은 동사(예: `درس`/`نجح`)를 여러 예문에서 반복하거나, **패턴 스크립트 표의 예문이 레퍼토리·드릴과 겹치는 것**도 피한다. 하나의 패턴 묶음(패턴+레퍼토리+드릴) 전체에서 동사·상황이 골고루 나오게 한다.
   - 문법 요소가 여러 개인 패턴(예: `لِـ`/`لِكَيْ`/`حَتَّى`)은 대표·표·레퍼토리·드릴에 걸쳐 그 요소들이 **골고루** 나오게 한다. 한 요소만 반복 금지.
   - 나쓰끼리도 완전히 같은 지문은 금지한다. 단, **같은 장면을 서로 다른 방언으로 비교**해 보여주는 것이 명확한 목적일 때만 예외로 한다.
10. **한국어도 자연스러워야 한다.** 한국어는 직역이 아니라 학습자가 장면을 바로 이해할 수 있는 말이어야 한다. 단, 패턴 구조를 일부러 보여줘야 하는 경우에는 어색하지 않은 범위에서 어순을 살릴 수 있다.

---

## 1. 아랍어 표기 규칙 (`arabic` 컬럼)

### 1.1 ATA 표기 원칙 (앱·연습·발음 기준 — 우리 내부 표준)

1. **격모음(إعراب) 미표기** — 명사의 마지막 격어미 모음을 표시하지 않는다.
   - 탄윈(ـٌ ـٍ) 사용 안 함
   - 마지막 격어미 모음(ـُ ـِ ـَ) 표시 안 함
   - `مُدَرِّس` ✓ / `مُدَرِّسٌ` ✗ — `هَذَا طَالِب` ✓ / `هَذَا طَالِبٌ` ✗
   - **예외 (2026-07-27 확정): 만숩 탄윈(ـً)은 나오는 모든 경우 표기하고, 음가(note)에도 `-an` 발음을 적는다.**
     `أَسْأَلُ سُؤَالًا`(asʔala suʔālan) ✓ / `سُؤَال` ✗ — `أَشْرَبُ قَهْوَةً`(qahwatan) ✓. 기존의 `شُكْرًا`·`غَدًا`·`كَثِيرًا`와 동일 원리.
     비한정 목적어, صار·كان류의 비한정 술어(`صَارَ الْجَوّ بَارِدًا`), 부사적 만숩이 모두 해당.
     단, 2변화 명사(ممنوع من الصرف — 색깔 `أَسْوَد`, 여성형 `حَمْرَاء`, 2변화 이름 `زَيْنَب` 등)는 만숩이어도 탄윈이 없으므로 무표기 유지.
     구어(GLF·LEV·EGY) 행은 해당 없음 — 구어 발음에 탄윈이 없으므로 굳어진 표현(`شُكْرًا` 등) 외에는 붙이지 않는다.
2. **지시대명사** — 대거 알리프(ـٰ) 없이 파트하로. `هَذَا` ✓ / `هٰذَا` ✗ — `ذَلِكَ` ✓ / `ذٰلِكَ` ✗
3. 동사의 서법 모음(예: `أَفْعَلُ` `سَأَرْجِعُ`의 마지막 ـُ)은 시트 기존 관행대로 **표기 유지**(명사 격어미 미표기와 구분).

### 1.2 ⚠️ 교재(인쇄본) 주의 — 격어미 생략의 위험 (만숩 탄윈은 §1.1 예외로 확정)

> **2026-07-27 업데이트**: 아래에서 제기된 "타동사+목적어 탄윈 누락" 문제는 §1.1의 만숩 탄윈 예외로 해소됨(전 산출물 공통 표기·발음). 교재 한정 완전정자(주격·속격까지) 정책만 계속 미확정.

ATA는 인지부담을 줄이려 격어미를 의도적으로 생략한다(휴지형). 이는 **앱·발음·연습용으로는 유지**한다. 그러나 **교재(인쇄 `ata144_original`)는 외부에 노출·검증되는 산출물**이라, 격 생략이 **비문(문법 오류)처럼 보일 소지**가 있는 구성은 피한다.

- **타동사 + 목적어**: `أُرِيد زَيْن` 은 ATA 표기상 맞지만(휴지형), 완전 정자로는 `أُرِيدُ زَيْنًا`(목적격 탄윈). 외부 시선에서 "탄윈 누락 = 오류"로 읽힐 수 있음.
- **참고 — 이름의 격**: 삼변화 이름은 목적격에 탄윈(`زَيْد` → `زَيْدًا`), 2변화(ممنوع من الصرف) 이름은 탄윈 없이 파트하만(`زَيْنَب` → `زَيْنَبَ`). 즉 "무함마드 → 무함마**단**"은 정자 기준 맞는 지적.
- **현 지침**: 교재에 들어갈 예문은 격 생략이 오류로 비칠 구성(타동사+무관사 목적어 등)을 **가급적 회피**. 회피 불가하면 그 행은 `ab`(교재 script만 숨김) 또는 `aa`(교재 행 자체 제외)로 앱 전용 처리 검토.
- **미확정**: "교재 한정 격 표기 정책"(교재만 완전 정자로 표기할지) 은 아직 확정 안 됨 → §8 검토 항목.

### 1.3 마크업 (arabic·korean·note·script 공통)

| 마크업 | 의미 | 비고 |
|---|---|---|
| `*단어*` | 색 강조 (학습 초점) | drillB에서는 `*X*`가 **빈칸**으로 렌더(가리기) |
| `_단어_` | 박스 강조 | |
| `\|` (파이프) | 명사문 시작어‖서술어 구분 (`css = nom`) | 행 `css`가 `nom`일 때만 분리자로 렌더 |
| 줄바꿈(Alt+Enter) | `<br>` | |
| 줄 시작 `- ` | 불릿 (`script`/요약) | |
| 줄 시작 `>` | 인용 (`script`/요약) | |

> 마크업 문자 `*` `_` 는 ID 슬러그 생성 시 제거된다(텍스트는 유지).

---

## 2. 음가 표기 규칙 (`note` 컬럼)

- **항상 소문자 + 발음구별부호.** 앱에서 **항상 표시**(의미 토글·한국어 가리기 영향 받지 않음 — 단, "한국어 가리기"가 음가도 함께 가리도록 통일된 앱은 그 규칙 따름. 표기 자체는 항상 입력).
- 사용 기호: 장모음 `ā ī ū` (필요시 `ē ō`) / 인두·후두 `ʕ`(ع) `ʾ`(ء, hamza) / 강세·치찰 `ṣ ḍ ṭ ẓ ḥ` / 이중자 `th dh sh kh gh`
- 예: `as-salāmu ʕalaykum`, `hādhā ṭālib`, `urīd zayn`
- **note는 ID·오디오 파일명의 원천**이다(§5). 작성 시 일관성 필수.

### 2.1 ⚠️ 슬러그 충돌 주의 (미니멀 페어)

현행 슬러그 규칙은 발음부호를 ASCII로 단순화한다: `ḥ→h, ṣ→s, ḍ→d, ṭ→t, ẓ→z` (§5 표). 그래서 **ح/ه, ص/س, ط/ت, ض/د, ظ/ز** 같은 최소대립쌍이 같은 id로 충돌할 수 있다.
- 예: `ḥaraj`(حَرَج)·`haraj`(هَرَج) → 둘 다 `rep_haraj` 로 충돌 → 오디오 파일 1개를 두 단어가 공유(오재생).
- **작성 시**: 같은 유닛/타입 안에서 음가 슬러그가 겹치지 않는지 확인. 겹치면 보고(슬러그 규칙 개정 또는 단어 교체 필요). 근본 해결(소문자 doubling 등)은 §8 검토 항목.

---

## 3. 타입별 작성 규칙

### 3.1 패턴 pattern (`ptnA` / `ptnB` / `ptn`)

- **목적**: 사용자가 보는 즉시 인지 → 바로 말하기. 설명은 최소.
- `ptnA`: 유닛 대표 패턴(사이드바 내비게이터 노출). 유닛당 1개 핵심.
- `ptnB`: 같은 유닛에 함께 노출되는 서브패턴(2–4개).
- `ptn`: 드릴탭에 입력된 패턴 항목(선생님앱 PATTERN 표시).
- 패턴 자체는 "덩어리(chunk)"로 인식되도록 — 번역·문법용어 나열식 설명 지양.

### 3.2 레퍼토리 repertory / 미쌀 mithāl (패턴 설명용 예문)

- **레퍼토리**: 패턴을 설명하는 **대표 예문**. 행의 `script` 값이 앱 노출 화면에서의 표준 설명 방식을 정의.
- **미쌀(mithāl)**: 레퍼토리와 함께 쓰는 패턴 설명용 예문. 인강·교재 설명파트에 노출.
- **패턴 문장과 완전히 같으면 안 된다.** 레퍼토리는 패턴을 다시 복사하는 자리가 아니라, 같은 구조를 다른 어휘·다른 장면으로 보여주는 자리다.
- 같은 패턴에 들어가는 레퍼토리 2개 이상은 서로 다른 문장이어야 한다. 같은 동사·같은 장소·같은 목적어를 반복해 "모양만 거의 같은" 세트가 되지 않게 한다.
- `ref_ptn`으로 어떤 패턴에 매칭되는지 지정(비우면 유닛 공통).
- `script` 작성: 산문 + 불릿(`- `) + 인용(`>`) 혼용 가능. 한국어 설명 줄과 아랍어 예문 줄을 섞을 때, 아랍어로 시작하는 불릿은 RTL로 렌더된다(작성 시 의도대로 줄 시작 언어 선택).

### 3.3 드릴 drill (`drillA` / `drillB`)

- **목적**: 배운 패턴에 어휘만 바꿔 **반복 말하기**. 한국어 해석·번역 금지.
- **핵심**: 그 예문이 **해당 유닛의 패턴을 잘 보여줄 수 있어야** 한다(패턴 학습 효과 최우선).
- 어휘 제약: §0 공통규칙(이전 배운 것 + 해당 유닛 expression·kalimat). 안 배운 글자·패턴·어휘 금지.
- **자연스러운 말이어야 한다.** 패턴에 맞더라도 실제로 거의 쓰지 않는 조합은 금지. 예: "여기 직원"처럼 소속·장소가 있어야 자연스러운 말은 반드시 그 맥락을 함께 넣는다.
- **패턴·레퍼토리와 겹치지 않는다.** 드릴은 패턴이나 레퍼토리에서 이미 본 예문을 반복하지 않고, 같은 패턴을 다른 어휘·다른 장면으로 연습하게 한다.
- **드릴끼리도 완전히 중복하지 않는다.** 같은 페이지나 같은 ref_ptn 안에서 동일 문장, 동일 이름만 바꾼 사실상 같은 문장, 같은 장소·목적어만 반복하는 세트는 피한다. 발음 대립·알파벳 노출처럼 의도적 반복이 필요할 때만 예외로 한다.
- **한 세트 안의 반복을 줄인다.** 같은 단어·같은 시작 글자·같은 문장 모양이 위아래로 붙어 학습 효과가 줄어들지 않게 한다. 발음 대립·알파벳 노출 등 명확한 목적이 있을 때만 의도적으로 반복한다.
- **부정·방언은 목적이 있을 때만 쓴다.** 예: 걸프 `مُو`, 레반트/이집트 `مِش`는 구어체 부정 비교가 목적일 때 사용한다. 같은 줄이나 같은 지문 안에서 방언을 섞지 않는다.
- **문법 부담을 갑자기 올리지 않는다.** `لَيْسَ`처럼 정확하지만 격/서술 구조가 새로 생기는 표현은 초반 드릴에서 피하고, "아니요 + 다른 정체 제시" 또는 해당 유닛에서 명시적으로 다루는 구어 부정으로 처리한다.
- `drillA`: 수업 노출 + 교재 연습파트.
- `drillB`: 수업 미노출. 추가 연습/앱용. `*X*` 마크업 → 빈칸(가리기)으로 렌더. 빈칸은 학습 초점 글자/어휘.
- `page` 열: 같은 유닛 내 같은 `page` = 한 화면(교재·교재앱·선생님앱 공통). 작성 시 한 화면에 묶을 예문을 같은 page 번호로. (정책 일반화는 검토 중 — `product-ata144.md` 이슈란)
- 빈 행(arabic 없음) 금지 — 유령 페이지/슬라이드 유발.

### 3.4 표현 expression (`exp`, `kalimat` 탭)

표현으로 다룰 수 있는 경우(아래 중 하나 이상):

1. **일반 규칙(패턴)으로 정형화하기 어려운** 표현. 또는 패턴화 가능해도 **빈출빈도에 비해 너무 어려운 표현** (예: شُكْرًا shukran 등) — 패턴 기다리지 말고 표현으로 먼저.
2. 관용어구뿐 아니라, **패턴화 어렵거나 가능해도 빈출 대비 너무 어려운 "문법"** 도 표현으로 알려줄 수 있음.
3. **매우 높은 빈출빈도**를 가진 표현들.
4. **그 표현 자체가 또 하나의 패턴으로 인식**될 수 있게 (확장·치환 가능한 형태로).

- 인사·관용표현 등(예: `as-salāmu ʕalaykum`, `al-ḥamdu lillāh`). 방언 표현은 `lahja` 코드 지정.
- 나스(지문)에서 어휘로 **사용 가능**한 두 타입 중 하나(다른 하나는 kalimat).
- 표현은 **문장 하나**가 아니라, 여러 장면에 끼워 넣을 수 있는 **기능 표현**이어야 한다. 예: `أَنَا هُنَا`는 문장/패턴이지 표현이 아니다.
- 표현은 기본적으로 **일반 예문과 달라야 한다.** 현재 배운 패턴과 kalimat를 조합하면 바로 만들 수 있는 평범한 문장은 `drill` 또는 `nass`로 보내고, `exp`로 올리지 않는다.
- 단, `أَرَاكَ غَدًا`처럼 구조는 단순해도 실제 대화에서 하나의 인사·마무리 표현처럼 굳어져 **압도적으로 자주 쓰이는 말**은 표현으로 둘 수 있다. 이 경우에도 단순 문장이라서가 아니라 "통째로 쓰이는 빈출 기능 표현"이라는 이유가 있어야 한다.
- **아직 배우지 않은 패턴이 핵심인 표현**이라도 빈출이고 지문 장면에 꼭 필요하면 `exp`로 선행 도입할 수 있다. 다만 이때는 문법 설명을 길게 하지 않고, 통째 표현으로 먼저 익히게 한다.
- `exp` 후보를 뽑을 때는 먼저 "이 말은 어떤 장면에서 통째로 나오는가?"를 확인한다. 대답·반응·인사·감사·사과·허락·거절·놀람·전화·가게/수업 상황처럼 대화 기능이 분명해야 한다.
- `هَلْ تَسْمَعُنِي؟`, `أَفْهَمُكَ جَيِّدًا`처럼 해당 유닛의 핵심 문법을 보여주는 문장은 원칙적으로 패턴/드릴이다. 같은 문장이 전화 시작 표현처럼 특별히 굳어져 쓰일 때만 표현 후보로 재검토한다.
- **기능이 겹치면 하나만 선택**한다. 예: "맞죠?"와 "맞아요"가 같은 수업 기능이면 둘 다 넣지 않는다.
- **지문에 붙을 장면이 있어야 한다.** 처음 만남, 이름 확인, 선생님 부르기, 수업 중 못 알아들음, 한 번 더 요청, 실례/부탁 등 실제 장면을 상상할 수 있어야 한다.
- **호칭은 사용 맥락을 구분한다.** `يَا أُسْتَاذ / يَا أُسْتَاذَة`는 수업·교실·선생님 호출에 가장 자연스럽다. 모르는 사람·종업원·서비스 상황은 `لَوْ سَمَحْت` 같은 표현이 더 안전하다.
- 방언 표현은 지역을 명확히 한다. 걸프 `مُو`, 레반트/이집트 `مِش`, 걸프 `هِنِي`, 레반트 `هُون`처럼 같은 기능이라도 지역성이 다른 경우 `lahja` 또는 한국어/스크립트에서 의도를 명확히 한다.

### 3.5 단어 kalimat (`kalimat`, `kalimat` 탭)

**선정 우선순위 (원칙):**
1. **빈출 순** 우선.
2. 그 중 **패턴을 설명하기 좋은 단어**를 먼저. (그 유닛 신규 글자를 잘 보여주면 가점)
3. **지문(nass) 구성에 필요한 단어**를 추가 보충.
4. 안 배운 글자 포함 단어 금지(누적 알파벳 그룹 내에서 구성).
5. 의미 중복 회피(예: `بَيْت`(집) 있으면 `دَار`(집) 중복).
6. ATA 표기 원칙 준수(탄윈·격어미 없음).

- 한 행 = **단어 1개**(문장·구 아님). 구상·기초어휘가 단어카드/이미지에 적합.
- 이름(사람명 등 alam)은 kalimat가 아니라 고유명사 — 지문에서 자유 사용 가능(§3.6).
- **대표형만 넣는다.** 타마르부타나 성별 대응을 가르치는 유닛이 아니라면 `مُدَرِّس، مُدَرِّسَة`처럼 남녀형을 한 행에 병기하지 않는다. 드릴에서 여성 주어가 필요할 때만 문장 안에서 변형한다.
- **표현과 단어를 중복 등록하지 않는다.** `exp`로 이미 기능을 가진 표현은 특별한 이유가 없으면 kalimat로 다시 넣지 않는다.
- **이름은 kalimat가 아니다.** 사람 이름은 `alam`으로 관리한다. 나라·도시 등 지명도 지명 학습 목적이면 별도 관리하거나 해당 유닛의 방침을 따른다.
- **예문에서 쓰기 어려운 단어는 신중하게 넣는다.** 단어 자체가 좋아도 해당 유닛 패턴에서 자연스러운 문장을 만들기 어렵다면 보류한다.
- **소속·맥락이 필요한 명사는 단어만으로 판단하지 않는다.** 예: `مُوَظَّف`는 "직원" 단독보다 "여기 직원", "회사 직원"처럼 맥락이 붙을 때 자연스러운 경우가 많다. 이런 단어를 쓰면 드릴·지문에도 자연스러운 맥락을 함께 설계한다.

### 3.6 지문 nass (`nass`, `nass` 탭)

**나스에 쓸 수 있는 단어는 딱 두 종류뿐:**
1. **`kalimat` 타입**으로 등록된 어휘
2. **`exp` 타입**으로 등록된 표현

- 해당 유닛에 kalimat가 없으면 → **이전 유닛까지 누적된 `exp`만** 사용.
- `drill`·`repertory`·`mithāl`·`summary`·`pattern` 타입에만 있는 단어는 **절대 사용 불가**.
- **이름(alam, 사람·지명 등 고유명사)은 사용 가능**(어휘 제약 예외).
- **여성 2인칭 금지 (2026-07-27 확정)**: 상대를 여성형으로 부르는 형태(ـِين 동사, ـتِ 완료형, أَنْتِ, 여성 접미 ـكِ, 여성 명령형 تَفَضَّلِي 등)는 지문·드릴·래퍼·스크립트 전체에서 쓰지 않는다. 여성 화자는 1인칭·중립 발화로만 등장할 수 있다(آسِفَة 등 1인칭 여성 표지는 허용). 여성 이름의 인물은 호명·2인칭 지시 대상이 되지 않게 구성한다. ※ A02~A08 인사 유닛의 남녀 인사쌍(كِيفَك/كِيفِك، أَنْتَ/أَنْتِ)은 커리큘럼 자체이므로 예외 여부 확정 전까지 기존 유지.
- **한국어 해석과 사용역 분리**: 표준어=존댓말, 구어=반말 대응 금지. 같은 문형 그룹(패턴·래퍼·드릴)은 높임 통일, 지문은 대화 맥락에 따라 자연스럽게 정하고 한 대화 안에서 일관 유지.
- **kalimat lahja 표기**: 단어의 lahja는 그 단어 자체의 사용역 기준(예: سَيَّارَة·مَطَار는 표준·구어 공용 → `MSA, GLF`). 구어 유닛에 등장했다는 이유로 GLF 단독 표기하지 않는다.

**지문 작성 기준:**

1. **일상적**일 것. §0 공통규칙대로 — 실제 아랍인들이 자기들끼리 쓰거나, 표준어로 외국인 배려해 **실제로 말할 법한** 지문.
2. **연결된 맥락보다 장면성이 중요하다.** 실제 대화 중 한 부분을 따온 듯한 느낌으로, **한 장면을 사용자가 하나의 패턴으로 인식**하게 만드는 것이 목적.
3. **해당 유닛의 패턴을 알 수 있는 내용**이 포함되어야 함.
4. 해당 유닛의 **표현(exp)이 포함되면 좋음**(필수 아님).
5. **배운 패턴·표현·kalimat 재활용** 권장 — **spaced repetition**이 가능하게 구성.
6. 같은 유닛 안에서도 **다양한 주제·패턴·표현·kalimat**가 쓰이게.
7. **나쓰끼리 완전 중복 금지.** 같은 아랍어 문장, 같은 한국어 장면, 같은 대화 흐름을 그대로 반복하지 않는다.
8. **비슷한 지문 허용 조건** — 거의 같은 구조에서 서로 다른 구어체(방언)를 비교해 보여주는 것이 목적일 때만 허용한다. 이 경우 `lahja`를 반드시 구분하고, 구어체 차이가 학습 포인트로 보여야 한다.
- **패턴을 억지로 모두 넣지 않는다.** 지문은 종합 시험이 아니라 실제 장면이다. 한 지문에는 하나의 장면과 하나의 핵심 패턴이 선명하면 충분하다.
- **직업·정체 확인은 실제 장면이어야 한다.** "그는 회계사예요? 아니요, 그는 엔지니어예요" 같은 문장이 드릴로는 가능해도 지문으로는 장면이 약할 수 있다. 접수, 교실, 처음 만남, 명단 확인 등 말이 나올 상황을 먼저 만든다.
- **대화는 보통 2줄을 기본으로 한다.** 긴 설명보다 짧은 왕복 대화로, 한 장면을 잘라낸 느낌을 우선한다.
- **방언 일관성 유지.** 한 지문 안에서는 표준어·걸프·레반트·이집트 표현을 임의로 섞지 않는다. 비교가 목적이면 별도 지문으로 분리하거나 `lahja`를 명확히 한다.
- **한국어도 실제 대화처럼 쓴다.** "그는 여기 직원이에요"처럼 한국어에서 자연스러운 표현을 쓰고, 지나치게 번역투인 "이분은 상인이에요" 같은 표현은 피한다.

- **ID 예외**: nass의 id 슬러그는 `note` 전체가 아닌 **첫 3단어만** 사용 → `nass_{첫3단어슬러그}`.

---

## 4. 한국어 (`korean` 컬럼)

- arabic을 부연하는 한국어 의미. 앱 노출·가리기 대상.
- 드릴은 번역 제공이 목적이 아님 — 한국어는 보조(이해 확인용). 패턴 학습 흐름을 번역식으로 만들지 말 것.
- 명사문 등에서 `|` 사용 시 한국어에도 같은 위치에 `|` (시작어‖서술어) — `css=nom`이면 분리자로 렌더.

---

## 5. ID·오디오 파일명 (작성자가 알아야 할 연결)

작성자는 `note`(+`type`)만 채우면 ID가 ARRAYFORMULA로 자동 생성된다. **ID = 오디오 파일명**이므로 note 작성이 곧 파일명 결정.

**기본 형식**: `{type}_{slug(note)}` (units 탭만 `{U}_{slug(note)}`)

| 탭 | id 컬럼 | 형식 | 예 |
|---|---|---|---|
| units | id_unit | `{U}_{slug}` | `A01_ba-ta-tha-nun-ya` |
| pattern | id_ptn / id_ptn(no) | `ptn_{slug}` / `ptn{NNN}_{slug}` | `ptn_ba-bu-bi` |
| drill | id_drill | `{type}_{slug}` | `drilla_banata` |
| kalimat | id_kalimat | `{type}_{slug}` | `exp_as-salamu`, `kalimat_ana` |
| nass | id_nass | `nass_{첫3단어slug}` | `nass_as-salamu-alaykum` |

**슬러그 절차**: ① `* _` 제거 ② 음가부호 `' ʿ ʾ ʕ` 제거 ③ 구두점 `. , ! ?` 제거 ④ 소문자 ⑤ 디아크리틱→ASCII(`ā→a ī→i ū→u ē→e ō→o ḥ→h ṣ→s ḍ→d ṭ→t ẓ→z`) ⑥ 공백/하이픈→단일 `-` ⑦ 양끝 `-` 제거. (nass는 ①~③ 후 첫 3단어 추출 → ④~⑦)

**오디오 경로**: `gs://all-that-arabic-14/audio/{U}/{id}.mp3` (실패 시 `.wav`). 파일명이 시트 id와 **정확히** 일치해야 재생됨(대소문자·하이픈·철자 1자라도 다르면 404).

⚠️ §2.1 슬러그 충돌(미니멀 페어) 주의.

---

## 6. 교재(인쇄) ↔ 교재앱 노출 제어 (`css` 값으로 작성 단계 결정)

| css | 인쇄 교재 | 교재앱 | 쓰는 경우 |
|---|---|---|---|
| `ab` | 행 표시, **script만 숨김** | script 포함 노출 | 앱에서만 부연설명 더 줄 때 |
| `aa` | **행 자체 제외** | 행 노출 | 앱 전용 보충 예문(인쇄 불필요) |

→ §1.2의 "교재 격어미 비문 소지" 회피책으로도 활용: 문제 소지 예문은 `aa`/`ab`로 앱 전용 처리.
렌더·기타 css(`nom`·`baseline`·`vlist`·`five`·`six` 등) 정의는 `product-ata144-design.md` §8.

---

## 7. status (작성→노출 단계)

- `confirmed`: 검수 완료 → 배포본 노출
- `draft`: 작업 중 → (1.4.4 textbook) dev 미리보기에서만, 배포본 숨김 / (1.4.3 복습앱 app143s) **빈값·confirmed만 노출**이므로 draft = 완전 숨김
- 빈값·기타: 1.4.4는 숨김 / 1.4.3 복습앱은 빈값이면 노출 (앱별 규칙 상이 — CLAUDE.md "status 필터" 참조)
- 작성 후 검수 전까지 `draft`, 검수·확정 시 `confirmed`로 승격.

---

## 8. 검토 중 / 미확정 (정책 결정 대기)

| # | 항목 | 현재 | 결정 필요 |
|---|---|---|---|
| 1 | **교재 격어미 표기** | 앱·연습은 휴지형(격 생략) 유지. 교재는 비문 소지 구성 회피 권고 | 교재 한정 완전 정자(격 표기) 정책을 둘지, 회피만으로 갈지 |
| 2 | **슬러그 미니멀페어 충돌** | `ḥ→h` 등 단순화로 ح/ه·ص/س 등 같은 id 충돌 가능 | 소문자 doubling(ḥ→hh) 등 무손실 ASCII 규칙 도입 여부 |
| 3 | **드릴 `page` 일반원칙** | A03·A04 시범 적용. 1·2과 미적용 | 이전 유닛 소급·빈 page 처리 등 일반 규칙 확정 |
| 4 | 앱별 status 규칙 통일 | 1.4.4 textbook(draft=dev) vs 1.4.3 복습앱(빈값=노출) 상이 | 통일 여부 |

---

### 출처
이 문서는 `product-ata144.md`(원칙·타입·ID·구조), `CLAUDE.md`(아랍어 표기 원칙·나스 작성 규칙·status·ID), 및 작업 대화에서 합의된 규칙을 종합·정리한 것이다. 렌더/CSS는 `product-ata144-design.md`를 정본으로 한다. 충돌 시 본 문서(작성 규칙)·design.md(렌더)·CLAUDE.md(운영)를 각 영역의 정본으로 본다.
