# 올댓아라빅 All That Arabic 1.4.4
---

### 이론적 배경 Theoretical Background

| 방식 | 이름 | 핵심 접근 | 도구 |
|---|---|---|---|
| 1방식 | 구조화 structural | 문장 성분 → 트리구조 → 논리적 이해 | 문법맵핑, 연습문제, 작문 |
| 2방식 | 행동주의 behaviorism | 현지에서 배우듯, 모국어 습득하듯 | 스토리텔링, 연상, 토론, 활동 |
| **3방식** | **인지주의 cognitivism** | **패턴을 '습득'한 후 어휘를 바꿔가며 유추해 '말하게' 유도** | **패턴드릴, 단편말하기, 유추, 훈련** |
| 4방식 | 완전한 개인화 customized | 커리큘럼 불필요 | consulting 외 교육 도구 불필요 |

1방식은 언어를 규칙의 조합으로 보고 문장을 분해하여 이해시킨다. 논리적으로 정교하지만, 실제 발화 상황에서 학습자는 이 규칙들을 실시간으로 조립할 여유가 없다. 언어에 대한 지식은 생기지만 언어를 사용하는 능력은 만들어지지 않는다. 2방식은 어린아이가 모국어를 배우듯 풍부한 맥락과 대량 노출을 통해 포괄적인 방법으로 언어를 익히게 한다. 현지 몰입 환경에서는 강력하지만, 하루에 제한적인 시간만 외국어 학습에 할애하는 성인에게는 비현실적이다. 성인의 뇌는 이미 모국어에 최적화되어 있어, 짧은 노출만으로 언어 규칙을 스스로 유추하고 내재화하기 어렵다.

**제품의 목적은 아랍어 교육에 3방식을 도입하는 것이다**. 자주 쓰이는 아랍어 구문을 분석하지 않고 하나의 덩어리로 인식시킨 뒤, 그 패턴 안에서 어휘를 바꿔가며 반복해서 말하게 한다. 제한된 학습 시간 안에 언어를 각인시키려면 넓고 얕은 노출보다 좁고 깊게 패턴을 타격하는 훈련이 효율적이다. 패턴이 입에 붙으면 새 패턴이 이전 패턴과 결합되고, 이 결합이 반복될수록 더 긴 문장을 자동적으로 말할 수 있게 된다. 해석하는 것이 아니라 말하는 것이 목표다.

3방식이 완성되면 4방식으로의 이행이 가능해진다. 패턴과 어휘의 데이터베이스가 충분히 쌓이고 학습자의 특성과 취약점이 파악될 때, 정해진 커리큘럼 없이 학습자 개인에게 최적화된 패턴과 드릴을 실시간으로 제공할 수 있다. 올댓아라빅이 지금 구축하고 있는 모듈화된 콘텐츠 구조는 그 전환을 위한 기반이다.


### 원칙tenet
올댓아라빅은 아랍어의 어형(sarf)과 구문(nahw)을 패턴화하고, 학습자가 이를 하나의 덩어리(chunk)로 인식한 뒤 직접 말하는 것을 통해 자연스럽게 체득하도록 디자인하고 있다. 인지 부담을 최소화하고, 배운 패턴에 어휘를 바꿔 넣어 스스로 문장을 만들고 말할 수 있게 되는 것이 핵심 목표다.

핵심원칙은 1. 핵심 내용을 학습자가 바로 인지 할 수 있어야하며 2. 실제 아랍인들이 쓰는 표현만 다루며(교재에만 있는 말은 쓰지 않는다) 3. 패턴·어휘·예문·지문 등 구조화된 데이터베이스로 구축되어야 하며 이렇게 모듈화된 데이터 포맷은 4방식 개인화로 업데이트 하는 기반이 된다. 아울러 4. 빠르고 저비용으로 제작해 제한된 시간 안에 시제품 제작 - 제품검증 - 피드백 루프를 만들 수 있게 생산성이 담보되어야 한다.

- **패턴pattern**, 사용자가 보는 즉시 인지하고 바로 '말하기'를 할 수 있어야 한다. 설명은 패턴을 이해하는 데 필요한 최소한으로만 한다.
- **드릴drill**, 배운 패턴을 사용자가 말하기 위한 것이다. 아랍어 문장을 한국어로 해석하거나 번역하는 방식을 배제한다. 배웠던 패턴에 어휘를 바꿔 반복해서 말하도록 유도한다.
- **표현expression**, 빈출 어휘와 표현 중 패턴으로 정형화하기 어렵거나, 패턴으로 다룰 수 있어도 지금 당장 빠르게 익혀야 하는 것들을 따로 다룬다.
- **유추inference**, 배운 패턴을 스스로 떠올리고 적용할 수 있는 다양한 문제를 제공한다. 맥락 안에서 스스로 생각하고 말하게 유도하는 것이 목적이며, 이 과정이 실질적인 언어 운용 능력을 만든다.
- **단어kalimat**, 빈출 순으로 우선 선정하되, 패턴을 설명하기 좋은 단어를 먼저 고르고, 지문(nass) 구성에 필요한 단어를 추가로 보충한다.
- **내러티브narrative**, 선생님의 재량 영역으로 제품과 강의를 보다 유연하게 만들고, 아랍어·아랍인·아랍 문화에 대한 언어적·문화적 배경지식을 제공하는 이야깃거리를 선정해 활용한다.


> ⚠️ **드릴·표현·지문 제작 원칙**
> 드릴, 표현, 유추활동을 위한 지문을 만들 때는 반드시 **실제 아랍인들이 쓰는 자연스러운(طَبِيعِيّ, ṭabīʕī)** 문장이어야 한다. 아직 배우지 않은 패턴과 어휘(kalimat)가 포함되어서는 안 된다.
> 단, **해당 유닛에서 새로 도입되는 kalimat와 expression은 활용 가능**하다.
>
> 📝 **타입별 상세 작성 규칙**(아랍어 표기·음가·ID·교재 격어미 주의·단어 선정 등)은 별도 문서 **`product-ata144-content-rules.md` (예문작성 규칙)** 를 정본으로 한다.

### 구조structure

1.4.4 데이터시트에는 패턴·레퍼토리·드릴·표현·지문·어휘·내러티브가 입력되며, 이 데이터를 기반으로 앱과 콘텐츠가 만들어진다. 선생님용 앱은 온라인/오프라인 수업 현장에서 선생님이 직접 조작하며 수업을 진행하는 도구이며 학생용 앱은 사용자가 접속해 배운 패턴을 반복 연습하는 용도로 쓴다(이후 말하기가 가능한 형태로 업데이트). 콘텐츠는 세 종류로 다르스는 패턴·드릴 데이터를 기반으로 제작하는 녹화 강의다. 칼럼은 표현 데이터를 활용한 유튜브 콘텐츠로, 표현 하나를 중심으로 파생 표현과 예문을 묶어 전달한다. 깔람은 내러티브 데이터를 기반으로 아랍어·아랍인·아랍 문화를 주제로 한 콘텐츠로 구성될 예정이다.

- 데이터시트datasheet(1.4.4)
	- 앱application
		- 선생님용 앱(app_teacher)
		- 사용자용 앱(app_user)
	- 콘텐츠contents
		- 다르쓰darss
		- 칼람kalam
		- 깔람qalam

**데이터시트 구조상 표시되는 열을 다음과 같이 정의한다.**

- **C** — 챕터명. 각 챕터는 하나의 공통 테마를 공유하며, 정규수업 기준 한 챕터가 1개월(8회 수업)을 구성하는 것을 가정한다.
- **U** — 유닛 번호. 한 유닛은 하나의 대표 패턴을 가지며, 여기에 따라올 수 있는 2–4개의 서브패턴을 포함할 수 있다.
- **arabic** — 해당 양식(패턴·드릴·표현·지문·어휘 등)에 따라 사용자가 배울 핵심 내용을 표시한다. 앱에 노출되며 가릴 수 있다.
- **korean / note** — arabic을 부연하는 한국어 의미와 참고사항. 앱에 노출되며 가릴 수 있다.
- **type** — 해당 데이터의 성격을 정의한다. 후술한다.
- **key** — 해당 데이터의 고유 식별자.
- **lahja** — 지역(방언) 코드. 기본값은 MSA이며 내용에 따라 구별된다.
- **script** — 레퍼토리 고유의 표준화된 설명 방식.
- **url** — 해당 데이터 파일의 위치. 앱과 연동된다.
- **css** — 앱에 노출되는 데이터의 고유 표시 양식을 지정한다.
- **중복** — 데이터의 활용 빈도를 추적한다.

<svg viewBox="0 0 760 430" xmlns="http://www.w3.org/2000/svg" style="width:100%;max-width:760px;" font-family="'Apple SD Gothic Neo','Noto Sans KR',sans-serif;">
  <defs>
    <marker id="arr" markerWidth="8" markerHeight="8" refX="6" refY="3" orient="auto">
      <path d="M0,0 L0,6 L8,3 z" fill="#333"/>
    </marker>
    <marker id="arr-d" markerWidth="8" markerHeight="8" refX="6" refY="3" orient="auto">
      <path d="M0,0 L0,6 L8,3 z" fill="#888"/>
    </marker>
  </defs>
  <rect x="218" y="30" width="272" height="200" rx="8" fill="white" stroke="#333" stroke-width="1.5"/>
  <text x="354" y="56" text-anchor="middle" font-size="13" font-weight="700" fill="#111">데이터시트</text>
  <line x1="232" y1="64" x2="476" y2="64" stroke="#bbb" stroke-width="1"/>
  <rect x="232" y="74" width="120" height="28" rx="4" fill="white" stroke="#bbb" stroke-width="1"/>
  <text x="292" y="93" text-anchor="middle" font-size="11" fill="#333">패턴</text>
  <rect x="362" y="74" width="116" height="28" rx="4" fill="white" stroke="#bbb" stroke-width="1"/>
  <text x="420" y="93" text-anchor="middle" font-size="11" fill="#333">드릴</text>
  <rect x="232" y="112" width="120" height="28" rx="4" fill="white" stroke="#bbb" stroke-width="1"/>
  <text x="292" y="131" text-anchor="middle" font-size="11" fill="#333">레퍼토리</text>
  <rect x="362" y="112" width="116" height="28" rx="4" fill="white" stroke="#bbb" stroke-width="1"/>
  <text x="420" y="131" text-anchor="middle" font-size="11" fill="#333">어휘 / 표현</text>
  <rect x="232" y="150" width="120" height="28" rx="4" fill="white" stroke="#bbb" stroke-width="1"/>
  <text x="292" y="169" text-anchor="middle" font-size="11" fill="#333">지문</text>
  <rect x="362" y="150" width="116" height="28" rx="4" fill="white" stroke="#bbb" stroke-width="1"/>
  <text x="420" y="169" text-anchor="middle" font-size="11" fill="#333">내러티브</text>
  <text x="105" y="26" text-anchor="middle" font-size="11" fill="#555">앱</text>
  <rect x="15" y="35" width="185" height="68" rx="6" fill="white" stroke="#333" stroke-width="1.5"/>
  <text x="107" y="57" text-anchor="middle" font-size="12" font-weight="700" fill="#111">선생님용 앱</text>
  <text x="107" y="74" text-anchor="middle" font-size="10" fill="#444">온라인 / 오프라인 수업</text>
  <text x="107" y="90" text-anchor="middle" font-size="10" fill="#444">선생님이 직접 조작</text>
  <rect x="15" y="120" width="185" height="68" rx="6" fill="white" stroke="#333" stroke-width="1.5"/>
  <text x="107" y="142" text-anchor="middle" font-size="12" font-weight="700" fill="#111">사용자용 앱</text>
  <text x="107" y="159" text-anchor="middle" font-size="10" fill="#444">배운 패턴 반복 연습</text>
  <text x="107" y="175" text-anchor="middle" font-size="10" fill="#444">말하기 기능 추가 예정</text>
  <line x1="218" y1="88" x2="202" y2="78" stroke="#333" stroke-width="1.5" marker-end="url(#arr)"/>
  <line x1="218" y1="145" x2="202" y2="154" stroke="#333" stroke-width="1.5" marker-end="url(#arr)"/>
  <text x="625" y="26" text-anchor="middle" font-size="11" fill="#555">콘텐츠</text>
  <rect x="530" y="35" width="185" height="68" rx="6" fill="white" stroke="#333" stroke-width="1.5"/>
  <text x="622" y="57" text-anchor="middle" font-size="12" font-weight="700" fill="#111">다르스</text>
  <text x="622" y="74" text-anchor="middle" font-size="10" fill="#444">녹화 강의</text>
  <text x="622" y="90" text-anchor="middle" font-size="10" fill="#444">패턴 · 드릴 기반</text>
  <rect x="530" y="120" width="185" height="68" rx="6" fill="white" stroke="#333" stroke-width="1.5"/>
  <text x="622" y="142" text-anchor="middle" font-size="12" font-weight="700" fill="#111">칼람</text>
  <text x="622" y="159" text-anchor="middle" font-size="10" fill="#444">유튜브 콘텐츠</text>
  <text x="622" y="175" text-anchor="middle" font-size="10" fill="#444">표현 + 파생표현 · 예문</text>
  <rect x="530" y="205" width="185" height="68" rx="6" fill="white" stroke="#333" stroke-width="1.5"/>
  <text x="622" y="227" text-anchor="middle" font-size="12" font-weight="700" fill="#111">깔람</text>
  <text x="622" y="244" text-anchor="middle" font-size="10" fill="#444">유튜브 콘텐츠</text>
  <text x="622" y="260" text-anchor="middle" font-size="10" fill="#444">아랍어 · 문화 내러티브</text>
  <line x1="490" y1="88" x2="528" y2="69" stroke="#333" stroke-width="1.2" marker-end="url(#arr)"/>
  <line x1="490" y1="126" x2="528" y2="154" stroke="#333" stroke-width="1.2" marker-end="url(#arr)"/>
  <line x1="490" y1="164" x2="528" y2="239" stroke="#333" stroke-width="1.2" marker-end="url(#arr)"/>
  <line x1="715" y1="69"  x2="730" y2="69"  stroke="#999" stroke-width="1" stroke-dasharray="4,3"/>
  <line x1="715" y1="154" x2="730" y2="154" stroke="#999" stroke-width="1" stroke-dasharray="4,3"/>
  <line x1="715" y1="239" x2="730" y2="239" stroke="#999" stroke-width="1" stroke-dasharray="4,3"/>
  <line x1="730" y1="69"  x2="730" y2="239" stroke="#999" stroke-width="1" stroke-dasharray="4,3"/>
  <polyline points="730,154 750,154 750,380 107,380 107,190" fill="none" stroke="#999" stroke-width="1" stroke-dasharray="4,3" marker-end="url(#arr-d)"/>
  <rect x="305" y="371" width="175" height="18" rx="6" fill="white" stroke="#bbb" stroke-width="1" stroke-dasharray="3,2"/>
  <text x="392" y="384" text-anchor="middle" font-size="9" fill="#888">콘텐츠 → 사용자용 앱 통합 예정</text>
</svg>

### 타입type
type은 해당 데이터의 성격을 정의한다.

- **패턴pattern**
	- **ptn** — 드릴탭에 입력된 패턴 항목. 선생님용 앱 화면에 **PATTERN**으로 표시된다.
	- **ptnA** — pattern탭에 입력된 핵심 패턴. 해당 유닛의 대표 패턴을 나타내며, 사이드바 내비게이터에 표시된다.
	- **ptnB** — 따라오는 패턴. 같은 유닛에 함께 노출되는 서브패턴을 나타낸다.

- **드릴drill** — 사용자가 배웠던 패턴을 반복 연습하기 위한 것이다. 패턴 활용에 가장 적합한 어휘를 선정해 사용한다. 이전 유닛에서 이미 배운 kalimat·expression, 또는 해당 유닛에서 새로 도입되는 kalimat·expression만 사용 가능하다. 관측에 따라 변경될 수 있다.
	  - **레퍼토리repertory** — 패턴을 설명하기 위한 대표 예문을 나타낸다. script 값이 앱에 노출되는 화면에서의 표준 설명 방식을 정의한다.
	  - **미쌀mithāl** — 레퍼토리와 함께 쓰이는 패턴 설명용 예문이다. 인강과 교재 설명파트에 노출된다.
	  - **드릴drillA** — 수업에 노출되며 교재 연습파트에 해당한다.
	  - **드릴drillB** — 수업에는 미노출이며 추후 추가 연습문제 또는 사용자용 앱에 활용 예정이다.

- **표현expression** — kalimat탭에 입력된 표현 항목. type = `exp`, status = confirmed인 항목만 선생님용 앱에 노출된다.

- **나스nass** — nass탭에 입력된 지문 항목. status = confirmed인 항목만 선생님용 앱에 노출된다.

### ID 산출 규칙 v2 (1.4.4)

데이터시트의 모든 행은 자동 생성되는 고유 식별자(ID)를 가진다. 시트 입력자는 `note`(또는 `transliteration`)와 `type`만 채우면 ID 컬럼이 ARRAYFORMULA로 자동 채워진다.

**기본 형식**

```
{type}_{slug(note)}
```

`type`은 시트의 type 컬럼 값(소문자), `slug(note)`는 note 값을 다음 절차로 정규화한 문자열이다.

> ⚠️ **`units` 탭은 예외** — `{U}_{slug(note)}` 형식 사용 (예: `A01_ba-ta-tha-nun-ya`). 다른 탭의 `ref_unit`도 같은 형식으로 자동 산출되며, units 탭을 VLOOKUP해 매칭한다.

**슬러그 변환 절차**

| # | 단계 | 설명 |
|---|---|---|
| 1 | 마크업 제거 | `*` `_` 문자만 제거 (`b*a*` → `ba`, `_text_` → `text`). 안의 텍스트는 유지 |
| 2 | 음가 부호 제거 | `'` `ʿ` `ʾ` `ʕ` |
| 3 | 구두점 제거 | `.` `,` `!` `?` |
| 4 | 소문자 통일 | LOWER |
| 5 | 디아크리틱 → ASCII | ā→a, ī→i, ū→u, ē→e, ō→o, ḥ→h, ṣ→s, ḍ→d, ṭ→t, ẓ→z |
| 6 | 공백·하이픈 정규화 | 모든 공백류(스페이스/탭/개행)와 기존 하이픈을 단일 `-`로 압축 |
| 7 | 양끝 하이픈 제거 | 결과 시작/끝의 `-` 정리 |

**나스(nass) 예외** — `note` 전체가 아닌 **첫 3단어만** 슬러그 입력으로 사용. 1~3 처리 후 첫 1~3단어 추출, 그 다음 4~7 적용.

**탭별 산출 결과**

| 탭 | id 컬럼 | 형식 | 예시 |
|---|---|---|---|
| `units` | `id_unit` | `{U}_{slug}` ⚠️ 예외 — 유닛코드 사용 | `A01_ba-ta-tha-nun-ya` |
| `pattern` | `id_ptn` | `ptn_{slug}` | `ptn_ba-bu-bi` |
| `pattern` | `id_ptn(no)` | `ptn{NNN}_{slug}` (시트 전체 누적 일련번호) | `ptn002_ba-bu-bi` |
| `repertory` | `id_rep` | `rep_{slug}` | `rep_ba` |
| `drill` | `id_drill` | `{type_lower}_{slug}` | `drilla_banata`, `drillb_…`, `writing_…` |
| `expression` | `id_kalimat` | `{type_lower}_{slug}` | `exp_as-salamu-alaykum` |
| `nass` | `id_nass` | `nass_{slug_from_first_3_words}` | `nass_as-salamu-alaykum-wa-alaykumu` |
| `kalimat` | `id_kalimat` | `kalimat_{slug}` | `kalimat_ana` |

**`ref_unit` 자동 산출** — 모든 탭의 `ref_unit` 컬럼은 같은 행의 `U` 값을 키로 `units` 탭을 VLOOKUP하여 `id_unit` 값을 반환한다. 시트 입력자는 `U` 컬럼만 채우면 `ref_unit`은 자동.

**`id_ptn(no)` — 패턴 일련번호** — 시트 전체에서 type이 `ptn*`인 행만 골라 1부터 누적 카운트. 3자리 zero-pad. 학습자에게 보이는 식별자(교재 페이지에 표시).

**구글시트 ARRAYFORMULA** — 각 탭의 id 컬럼 헤더 셀(H1, pattern은 I1도)에 ARRAYFORMULA를 입력하면 데이터 행이 늘어도 자동으로 채워진다. 시트 입력자는 ID 컬럼을 직접 건드리지 않는다. 자세한 수식은 `/tmp/ata_apply_id_formulas.py`, `/tmp/ata_fix_unit_id.py` 참고.

**중복 검출** — 각 탭의 id 컬럼에 조건부 서식 적용:

```
범위:  H2:H        (pattern은 H2:H + I2:I)
조건:  맞춤 수식 → =AND($H2<>"", COUNTIF($H$2:$H, $H2)>1)
서식:  빨간 배경
```

같은 탭 안에서 동일 ID가 2개 이상 생기면 빨갛게 표시. 슬러그 충돌(다른 행에서 동일 note 생성)을 즉시 발견할 수 있다.

**파일명 규칙**

오디오 파일은 스토리지의 `audio/{unit_code}/` 폴더 안에 ID와 동일한 이름으로 저장한다. `unit_code`는 시트의 `U` 값이며, 파일명은 각 행의 ID 값과 정확히 일치해야 한다.

```
audio/{U}/{id}.mp3
audio/{U}/{id}.wav

예: audio/A01/ptn_ba-bu-bi.wav
```

앱은 항상 현재 유닛 코드와 행 ID를 조합해 직접 URL을 요청한다.

```
https://storage.googleapis.com/all-that-arabic-14/audio/{U}/{id}.mp3
https://storage.googleapis.com/all-that-arabic-14/audio/{U}/{id}.wav
```

먼저 `.mp3`를 시도하고 실패하면 `.wav`를 시도한다. 같은 ID라도 유닛 폴더가 다르면 다른 오디오 파일로 취급된다.

**아랍어 탭 → 오디오 매칭** — 교재용 앱에서 아랍어 텍스트를 누르면 해당 행의 `U`와 ID로 URL을 조합해 재생한다. 선생님용 앱에서는 슬라이드의 재생 버튼 또는 `S` 키로 같은 규칙의 오디오를 재생한다. 시트의 `url` 컬럼이 채워져 있으면 그 값을 우선 사용할 수 있다.

**TSS 발음 스크립트** — 시트의 `tss` 열은 앱 표시용 텍스트가 아니라 오디오 생성을 위한 발음 지시문이다. 오디오 생성기는 `tss` 값이 있으면 이를 최우선 TTS 입력으로 사용하며, `--only-tss` 옵션으로 `tss`가 있는 행만 로컬 `audio/{U}/`와 GCS에 다시 생성할 수 있다.

---

### 앱 Application

데이터시트에 입력된 콘텐츠는 두 개의 앱으로 노출된다. 선생님용 앱과 사용자용 앱은 같은 데이터를 공유하지만 용도와 디자인이 다르다. 선생님용 앱은 오프라인·온라인 수업 현장을 가정하며 TV나 모니터 등 가로로 넓은 화면에 최적화되어 있고, 선생님이 수업 흐름에 맞게 직접 조작하며 진행하는 도구다. 사용자용 앱은 복습을 가정하며 모바일 화면에 최적화되어 있고, 사용자가 혼자 접속해 배운 패턴을 반복 연습하는 용도로 쓴다. 두 앱 모두 슬라이드 방식으로 콘텐츠가 표시되며, 아랍어·한국어를 필요에 따라 가릴 수 있다. 사용자용 앱에는 말하기 기능이 추가될 예정이다.

**알파버전 앱의 데이터 연결 방식**은 다음과 같다. 앱은 단일 HTML 파일로, 별도 서버 없이 브라우저에서 직접 실행된다. 페이지 로드 시 Google Sheets의 Visualization API(`gviz/tq`, JSONP 방식)로 시트를 불러온다. 불러온 데이터는 status 규칙(`confirmed`=항상, `draft`=dev에서만, 그 외 숨김)으로 추려내고, U열(유닛 코드) 기준으로 묶은 뒤 `buildSlides()` 함수가 패턴 → 레퍼토리 → 드릴 → 나스 순으로 슬라이드 배열을 생성한다. 오디오는 각 행의 `U` 값과 ID 값으로 GCS 경로를 직접 조합한다(`/audio/{U}/{id}.mp3`, 실패 시 `.wav` 폴백).

이 구조는 빠른 제작과 검증을 위한 알파 단계의 방식이다. 이후 마르카즈아라빅이 자체 제작하는 LMS 시스템과 연동하는 방향으로 업데이트할 예정이며, 그 단계에서 실제 제품화가 이루어진다.

- 선생님용 앱: 가로 대형 화면 / 수업 진행 / 슬라이드 조작
- 사용자용 앱: 모바일 최적화 / 자율 복습 / 스와이프 이동 / 말하기 기능 예정
- 기술 스택: 단일 HTML 파일, 외부 의존성 없음
- 데이터 로딩: Google Sheets Visualization API (JSONP), 4개 탭 병렬 로드
- 슬라이드 생성: `buildSlides()` — drill탭 + kalimat탭(exp) + nass탭을 병렬 로드 후 status=confirmed 필터, 시트 순서 그대로 슬라이드 배열 생성
- 오디오: `key` → GCS URL 직접 조합 → `.wav` 폴백

#### 기술 요구사항 및 인프라 연결 Technical Setup

이 섹션은 ATA 1.4.4 앱을 처음 보는 개발자나 AI가 전체 연결 구조를 파악할 수 있도록 작성했다.

**1. 앱 파일 및 배포 (GitHub)**

앱은 단일 HTML 파일 하나로 구성된다. 빌드 도구, 서버, 프레임워크 없음.

| 항목 | 내용 |
|---|---|
| 저장소 | https://github.com/rasheedpark/allthatarabic |
| 배포 방식 | GitHub Pages (main 브랜치 자동 배포) |
| 선생님용 앱 URL | https://rasheedpark.github.io/allthatarabic/ata144_teacher/ata144_teacher.html |
| 사용자용 앱 URL | https://rasheedpark.github.io/allthatarabic/ata144_student/ata144_student.html |
| 교재용 앱 URL | https://rasheedpark.github.io/allthatarabic/ata144_textbook/ata144_textbook.html |
| 교재 인쇄 원본 | https://rasheedpark.github.io/allthatarabic/ata144_original/ata144_original.html |
| **1.4.3 메인앱** (선생님용, legacy) | https://rasheedpark.github.io/allthatarabic/ata143_teacher/ata143_teacher.html |
| **1.4.3 복습앱** (학생용, legacy) | https://rasheedpark.github.io/allthatarabic/archive/app143s.html |
| 구URL 리다이렉트 | `/app144.html` → teacher, `/app144s.html` → student (redirect stub) |
| 인증 방식 | Personal Access Token (repo 권한) — macOS 키체인 또는 remote URL에 포함 |

**로컬 파일 위치** (모두 `ATA-14/` 루트 기준):

| 산출물 | 로컬 경로 |
|---|---|
| 1.4.4 교재 (인쇄 B5) | `ata144_original/ata144_original.html` |
| 1.4.4 메인앱 (선생님용) | `ata144_teacher/ata144_teacher.html` |
| 1.4.4 교재용 앱 (학생용) | `ata144_textbook/ata144_textbook.html` |
| 1.4.3 메인앱 (선생님용, legacy) | `ata143_teacher/ata143_teacher.html` |
| 1.4.3 복습앱 (학생용, legacy) | `archive/app143s.html` |

에셋 경로(앱 기준): `../assets/` (하위 폴더에서 참조).

`git push origin main` 하면 수분 내 GitHub Pages에 자동 반영된다.

> **1.4.3 복습용 앱 (legacy, 운영 중)**: 제목 "올댓아라빅 복습 · v1.43". summary 슬라이드 포함, 패턴별 요약 표시. 1.4.4 학생앱이 아직 유닛을 따라잡지 못한 동안 사용자 복습용으로 계속 운영한다. 1.4.4가 모든 유닛을 커버하면 폐기 예정. 신규 css 모드(`nom`, `baseline2`)도 1.4.4와 동일하게 적용되어 있다.

**2. 콘텐츠 데이터 (Google Sheets)**

앱은 서버 없이 브라우저에서 Google Sheets를 직접 읽는다. Google Visualization API (`gviz/tq`) JSONP 방식을 사용하며, 시트가 **"링크가 있는 모든 사용자"** 로 공개 설정되어 있어야 한다.

| 항목 | 내용 |
|---|---|
| 시트 ID | `1w7e0mLLgFhzU7Ixs6CUfzgrwUG6EEy8jHijXF5UJwY8` |
| 시트 URL | https://docs.google.com/spreadsheets/d/1w7e0mLLgFhzU7Ixs6CUfzgrwUG6EEy8jHijXF5UJwY8 |
| 읽기 엔드포인트 | `https://docs.google.com/spreadsheets/d/{SHEET_ID}/gviz/tq?tqx=out:json&sheet={탭명}&headers=1` |

**탭 구조**:

| 탭명 | 앱 로드 | 포함 타입 | 주요 컬럼 |
|---|---|---|---|
| `units` | ✅ 유닛 슬라이드 | unit | C, U, arabic, korean, note, type, status, id_unit |
| `pattern` | ✅ 내비게이터/패턴 슬라이드 | ptnA, ptnB, ptn | C, U, arabic, korean, note, type, status, id_ptn, id_ptn(no) |
| `drill` | ✅ 메인 슬라이드 | ptn, repertory, mithāl, drillA, drillB, summary, writing | C, U, arabic, korean, note, type, status, id_drill, ref_ptn, lahja, script, css, url |
| `kalimat` | ✅ 표현/단어 슬라이드 | exp, kalimat, kalimat+ | C, U, arabic, korean, note, type, status, id_kalimat |
| `nass` | ✅ 나스 슬라이드 | nass | C, U, arabic, korean, note, type, status, id_nass, ptn_key |

> **앱별 탭 사용**
> - **학생용**: `units`, `pattern`, `drill`, `kalimat`, `nass` (5개)
> - **선생님용**: 위 5개 + 추가로 `repertory`, `expression` 탭도 참조 (해당 탭이 있을 경우)

**status 노출 메커니즘 (유닛 여는 기준)**: 행의 `status`로 dev/prod 노출을 나눈다.

| status | dev(미리보기·localhost) | prod(배포·github.io) |
|---|---|---|
| `confirmed` | ✅ | ✅ |
| `draft` | ✅ (배포 전 검수용) | ❌ |
| 그 외(`inbox`·빈값 등) | ❌ | ❌ |

`draft`로 작업한 유닛/콘텐츠는 배포 전 dev에서 확인 → `confirmed`로 바꾸면 사용자(배포본)에 노출된다. dev 판정은 hostname(`localhost`/`127.0.0.1`/`[::1]`)·`file:` 기준. 유닛 목록(units 탭)·콘텐츠 행 모두 같은 `ok()` 규칙. pattern탭은 status 무관하게 내비게이터용으로도 로드하며 ptnA 중 노출 대상인 것만 사이드바에 표시된다.

Claude가 시트를 직접 읽고 쓰기 위한 별도 인증:
- 인증 방식: `gcloud auth login --enable-gdrive-access`
- 토큰 발급: `gcloud auth print-access-token`
- 헬퍼 스크립트: `~/.claude/sheets_helper.sh`

**3. 오디오 파일 (Google Cloud Storage)**

앱은 오디오 URL을 시트에서 읽지 않고, 각 행의 `U` 값과 ID 값으로 GCS 경로를 직접 조합한다.

| 항목 | 내용 |
|---|---|
| 버킷명 | `all-that-arabic-14` |
| 1.4.4 오디오 폴더 | `audio/{unit_code}/` |
| URL 패턴 | `https://storage.googleapis.com/all-that-arabic-14/audio/{unit_code}/{id}.mp3` |
| 폴백 | `.mp3` 실패 시 `.wav` 자동 시도 |
| GCS 콘솔 | https://console.cloud.google.com/storage/browser/all-that-arabic-14 |

`unit_code`는 시트의 `U` 값이고, `id` 값이 곧 파일명이다. 예: `U = A01`, `id_expression = exp_as-salamu-alaykum` → `audio/A01/exp_as-salamu-alaykum.wav`.

파일은 버킷에서 **공개 읽기(Public)** 로 설정되어야 한다.

**4. 파일 저장 위치 (Google Drive)**

| 항목 | 경로 / 링크 |
|---|---|
| 팀 드라이브 루트 | https://drive.google.com/drive/folders/1CUlbL3JH57MWZC1ViWU7Zt2y5HyTOQ3H |
| ATA-14 폴더 | 팀 드라이브 내 `제품products/ATA-14/` |
| 1.4.4 데이터시트 | ATA-14 폴더 내 `ATA-1.4.4.gsheet` |
| 제품문서 | ATA-14 폴더 내 `product-ata144.md` |

로컬 마운트 경로: `/Users/rasheedpark/Library/CloudStorage/GoogleDrive-someaugust17@gmail.com/공유 드라이브/마르카즈아라빅 팀 드라이브/제품products/ATA-14/`

**5. 현재 미완료 요구사항**

| 항목 | 상태 | 내용 |
|---|---|---|
| 오디오 폴더 경로 | ✅ 완료 | `audio/{unit_code}/` 경로로 반영됨 |
| 내비게이터 재설계 | ✅ 완료 | M키 토글, ptnA confirmed 필터, 상단 좌측 고정, 섹션 탭 우측 배치 |
| 표현/나스 슬라이드 로드 | ✅ 완료 | kalimat탭(exp)·nass탭 병렬 로드 추가 |
| 사용자용 앱 | ✅ 완료 | `app144s.html` 제작 완료. 모바일 최적화, 스와이프, ptn summary 표시, 레퍼토리 생략 |
| drillB 노출 여부 | 🔲 미결정 | 현재 앱에서 drillB 노출 여부 결정 필요 |
| LMS 연동 | 🔲 장기 | 마르카즈아라빅 자체 LMS 시스템 구축 후 연동 |

#### 디자인 코드 Design Tokens

**색상 팔레트**

| 변수 | 값 | 용도 |
|---|---|---|
| `--bg` | `#F2F4F6` | 배경 |
| `--surface` | `#ffffff` | 카드/패널 배경 |
| `--accent` | `#1D49FF` | 강조 (포인트 컬러) |
| `--accent-bg` | `rgba(29,73,255,0.08)` | 강조 배경 (하이라이트 박스) |
| `--text` | `#12205A` | 본문 텍스트 |
| `--sub` | `#888FAC` | 보조 텍스트·노트·비활성 버튼 |
| `--border` | `#D5DAE8` | 구분선 |

**타이포그래피**

| 요소 | 폰트 | 크기 |
|---|---|---|
| 아랍어 (기본) | Noto Sans Arabic 700 | `clamp(3rem, 9vw, 9rem)` |
| 아랍어 (문장형) | Noto Sans Arabic 700 | `clamp(2rem, 6vw, 6rem)` |
| 아랍어 (쓰기체) | Dongol / Noto Kufi Arabic | 동일 |
| 한국어 | Spoqa Han Sans Neo | `clamp(1.2rem, 3.5vw, 3.5rem)` |
| 노트 | Spoqa Han Sans Neo | `clamp(0.9rem, 2.2vw, 2.2rem)` |
| 타입 레이블 | Spoqa Han Sans Neo | `clamp(0.55rem, 0.85vw, 0.75rem)` |
| 사이드바 헤더 | Spoqa Han Sans Neo | `clamp(1.1rem, 2.5vw, 1.6rem)` |
| 인덱스 항목 | Spoqa Han Sans Neo | `clamp(0.75rem, 1.6vw, 1.1rem)` |
| 단축키 바 | Spoqa Han Sans Neo | `clamp(0.6rem, 0.9vw, 0.78rem)` |

행간: 아랍어 `1.6` / 한국어·노트 `1.4` / 단축키 리스트 `2.1`

**레이아웃**

메인 콘텐츠 영역은 `min(92vw, 1400px)`로 너비를 제한하고 화면 중앙에 배치한다. 수직 방향은 `flex column + justify: center`로 항상 화면 중앙에 정렬되며, 상하 패딩은 `clamp(60px, 10vh, 100px) / clamp(80px, 12vh, 120px)`이다. 4코너 UI 요소(패턴번호, 카운터, 저작권, 로고)는 `position: fixed`로 고정되며 clamp로 뷰포트에 따라 여백이 조정된다.

**주요 컴포넌트 (선생님용 앱)**

- **로딩 화면** — `--accent` 색상 배경 전체화면, 중앙에 흰색 로고(`ata-logo2.png`), 하단 `markazarabic 1.4.4 beta` 레이블(흰색, 투명도 0.75, letter-spacing 2.5px, 소문자)
- **진행바** — 상단 고정, 높이 3px, `--accent` 색상, 슬라이드 진행률에 따라 너비 변화
- **하이라이트 박스** — `border: 3px solid --accent` / `background: var(--accent-bg)` / `border-radius: 14px` / 패턴의 핵심 부분 강조용
- **사이드바** — 너비 `min(440px, 92vw)`, 좌측 슬라이드 인입, `border-right: 1px solid var(--border)`, `box-shadow: 4px 0 24px rgba(29,73,255,0.10)`
- **인덱스 항목 (`.idx-item`)** — `display:flex`, `gap:0.7rem`, `align-items:baseline`, `border-left: 4px solid transparent`, 활성 시 `--accent` border + `var(--accent-bg)` 배경. 아랍어(`idx-ar`)·한국어(`idx-ko`) 모두 `text-align:left`
- **인덱스 태그 (`.idx-tag`)** — `font-size: 0.72em`, `background: var(--accent-bg)`, `border-radius: 4px`, 대문자
- **상단 좌측 영역 (`#ui-tl`)** — `position:fixed`, `max-width:75vw`. 내부 구조: flex row로 ① ptnA 정보 영역(유닛번호 + 아랍어·한국어 가로 나란히) ② 섹션 탭 영역 순으로 배치
- **섹션 탭 (`.sec-tab`)** — `border: 1px solid --border`, `border-radius: 6px`, `font-size: clamp(0.5rem, 0.85vw, 0.68rem)`, `padding: 2px 9px`. 활성: `--accent` 배경 + 흰색 텍스트. 비활성(해당 타입 없음): `opacity: 0.2`, 클릭 불가
- **단축키 바** — 하단 중앙 고정, 투명 배경, 기본 `opacity: 0.6`, 활성 시 `opacity: 1`. 아이콘 없이 텍스트만 표시
- **단축키 뱃지 (`.kb`)** — `background: var(--bg)`, `border: 1px solid var(--border)`, `border-radius: 3px`
- **해석 토글 버튼** — `Space`키, 한국어·노트 텍스트 가리기/보기
- **본문 토글 버튼** — `A`키, 아랍어 텍스트 가리기/보기 (opacity 0 처리)

#### CSS 코드 CSS Codes

데이터 행의 `css` 열에 코드를 입력하면 해당 슬라이드에 고유한 화면 표시 양식이 적용된다. ptn처럼 슬라이드 구조가 별도로 정의된 핵심 타입은 타입 자체에 고유 CSS가 자동 적용되며, `css` 열을 통해 추가적인 커스터마이징이 가능하다. 각 코드의 상세 CSS 정의는 [부록: CSS 코드 정의](appendix_css(ata144).md)를 참고한다.

| css 코드 | 적용 대상 | 설명 |
|---|---|---|
| `ab` | 인쇄 교재 vs 교재앱 | **앱 전용 설명문**. 인쇄 교재(`ata144_original`)에서는 그 행의 `script`(설명문)만 숨기고 행(아랍어·한국어·음가)은 표시. 교재앱은 script 포함 정상 노출 |
| `aa` | 인쇄 교재 vs 교재앱 | **앱 전용 행**. 인쇄 교재에서는 그 행 자체가 아예 안 나옴(모든 타입). 교재앱은 정상 노출. `status=confirmed`여도 인쇄 제외(css 우선) |
| `three` | 드릴·래퍼토리 그리드 | `five`와 동일한 그리드 표 방식이되 **3열**(five는 5열). 칸이 넓어 글자 크기는 five와 동일(축소 없음). 교재앱 기준 |
| `six` | 드릴·래퍼토리 그리드 | `five`와 동일한 그리드 표 방식이되 **6열**(five는 5열). 칸이 좁아 글자만 1px 축소(아랍어 15px, 음가 9px). 교재앱 기준 |
| (그 외) | — | `baseline`·`baseline2`·`write`·`nom`·`vlist`·`five` 등 상세 정의는 디자인코드 문서 §8 / `appendix_css(ata144).md` 참고 |

#### 내비게이터 Navigator Logic

슬라이드는 시트에 입력된 순서 그대로 노출된다. 별도 재정렬 없이 시트 순서가 곧 화면 순서다.

**사이드바 (왼쪽 인덱스)**

- `M`키 또는 메뉴 버튼으로 열고 닫는다.
- pattern탭의 `ptnA` 타입 항목 중 `status = confirmed`인 것만 표시된다.
- 각 항목은 **아랍어 / 한국어**를 줄바꿈해 한 행으로 표시하며, 둘 다 왼쪽 정렬이다.
- 항목을 누르면 해당 유닛의 첫 슬라이드로 이동하고 사이드바가 닫힌다.

**상단 좌측 고정 영역**

어떤 슬라이드를 보고 있든 상관없이, 현재 유닛의 `ptnA` 아랍어와 한국어가 항상 고정 표시된다. 현재 슬라이드 내용이 아니라 해당 유닛의 대표 패턴이다.

```
[유닛번호]
[ptnA 아랍어]  [ptnA 한국어]    [패턴] [드릴] [표현] [칼리마트] [나스]
```

- 아랍어와 한국어는 나란히(왼쪽 정렬)로 배치된다.
- 섹션 탭은 ptnA 내용의 우측에 위치한다.

**섹션 탭 (상단 우측)**

현재 유닛 안에서 해당 타입의 첫 슬라이드로 점프한다. 해당 타입 슬라이드가 없으면 탭은 비활성(opacity 낮춤) 처리된다.

| 탭 | 매칭 타입 | 데이터 출처 |
|---|---|---|
| 패턴 Pattern | `ptn`, `ptna` | drill탭 |
| 드릴 Drill | `drilla` | drill탭 |
| 표현 Expression | `exp` | kalimat탭 |
| 칼리마트 Kalimat | `kalimat` | kalimat탭 |
| 나스 Nass | `nass` | nass탭 |

---

### 사용자용 앱 User App (app144s)

`app144s.html`은 학습자가 배운 패턴을 자율적으로 복습하기 위한 모바일 최적화 앱이다. 선생님용 앱(`app144.html`)과 같은 데이터시트를 공유하지만 표시 방식과 구성이 다르다.

**선생님용 앱과의 차이점**

| 항목 | 선생님용 (app144) | 사용자용 (app144s) |
|---|---|---|
| 화면 최적화 | 가로 대형 (TV·모니터) | 모바일 세로 (최대 640px) |
| 이동 방식 | 키보드 ← → | 스와이프 (좌우) + 하단 버튼 |
| 레퍼토리 | 노출 | **생략** |
| ptn 슬라이드 | arabic + korean + note | arabic + **summary** 컬럼 (설명 텍스트) |
| 미쌀(mithāl) | 레퍼토리와 동일 양식 | 동일 양식으로 표시 |
| 노출 타입 | ptn, repertory, mithāl, drillA, exp, kalimat, nass | ptn, mithāl, drillA, exp, kalimat, nass |
| 레이아웃 | position: fixed 4코너 | flex column, 100dvh, max-width 640px |

**노출 타입 (SHOW_TYPES)**

사용자용 앱에서 표시되는 타입: `ptn`, `mithal`, `drilla`, `exp`, `kalimat`, `nass`

레퍼토리(`repertory`)는 사용자용 앱에서 제외된다. 나머지 로직(status=confirmed 필터, 유닛 순서, 나스 포함)은 선생님용 앱과 동일하다.

**ptn 슬라이드 렌더링**

ptn 타입은 드릴탭의 `summary` 컬럼 내용을 설명 영역으로 표시한다. summary가 없을 경우 `scriptA` 컬럼을 대신 사용한다.

- 아랍어(`arabic`)가 있으면 상단에 작게 표시
- summary/scriptA 텍스트를 줄 단위로 파싱:
  - `- `로 시작하는 줄 → 불릿 항목 (점 + 텍스트)
  - 일반 줄 → 산문(prose) 단락
- 줄바꿈과 인라인 스타일(`**bold**`, `*italic*`, `[[arabic]]` 등) 지원

**모바일 레이아웃**

```
.app {
  display: flex; flex-direction: column;
  height: 100dvh; max-width: 640px;
  margin: 0 auto; background: var(--surface);
}
```

태블릿/데스크탑(660px 이상)에서는 상하 28px 여백과 border-radius를 추가해 카드형 레이아웃으로 표시된다.

**스와이프 제스처**

터치 시작(`touchstart`)과 종료(`touchend`) 좌표 차이로 슬라이드 이동을 감지한다.
- 수평 이동 42px 이상이고 수직 이동의 1.5배 이상이면 슬라이드 이동
- 왼쪽 스와이프 → 다음 슬라이드 / 오른쪽 스와이프 → 이전 슬라이드

**로딩 화면**

선생님용 앱과 동일: `--accent`(#1D49FF) 배경, 중앙 흰색 로고, 하단 `markazarabic 1.4.4 beta` 레이블.

**첫 방문 온보딩**

`localStorage` 플래그(`ata144s_onboarded`)로 첫 방문 여부를 확인한다. 첫 방문 시 조작법 안내 화면이 표시되며, 이후 방문 시 건너뛴다.

**주요 컴포넌트 (사용자용 앱)**

- **상단 바** — 유닛·슬라이드 번호 표시, 해석 토글 버튼
- **슬라이드 카드** — 스크롤 가능, 중앙 정렬, 하단 스와이프 힌트
- **하단 버튼** — 이전(`‹`) / 다음(`›`) 이동 버튼
- **사이드바** — 유닛 인덱스, 선생님용 앱과 동일한 구조
- **해석 토글** — 한국어·노트 텍스트 가리기/보기

---

### 참고References

#### 구글시트 인증 관련

Google Sheets API를 Claude가 직접 읽고 쓰기 위해 아래 방식으로 인증을 설정했다.

- **인증 방식**: `gcloud auth login --enable-gdrive-access`
  - 서비스 계정 키 없이 사용자 계정으로 직접 인증
  - 조직 정책(`iam.disableServiceAccountKeyCreation`)으로 서비스 계정 키 생성이 차단되어 있어 이 방식 채택
- **토큰 발급**: `gcloud auth print-access-token`으로 매 호출 시 토큰 사용
- **API 엔드포인트**: `https://sheets.googleapis.com/v4/spreadsheets/{SHEET_ID}/values/{range}`
- **헬퍼 스크립트**: `~/.claude/sheets_helper.sh`

**관련 리소스**
- 1.4.4 스프레드시트: https://docs.google.com/spreadsheets/d/1w7e0mLLgFhzU7Ixs6CUfzgrwUG6EEy8jHijXF5UJwY8
- Google Sheets API 문서: https://developers.google.com/sheets/api/reference/rest
- Google Cloud Console: https://console.cloud.google.com

---

### 앱 표시 로직 App Display Logic

**데이터 노출 기준**
선생님용 앱은 유닛별로 묶어 순서대로 표시하며, `status = confirmed`인 데이터만 노출된다. 화면 상단에 해당 행의 `type` 값이 작게 표시된다. 기본 화면 양식은 1.4.3과 동일하다.

**오디오 재생 로직**
앱은 기본적으로 행의 `U` 값과 ID 값으로 URL을 직접 조합한다. 시트의 `url` 컬럼이 채워진 경우에는 해당 URL을 우선 사용할 수 있다.

```
https://storage.googleapis.com/all-that-arabic-14/audio/{unit_code}/{id}.mp3
```

실패 시 `.wav`로 폴백한다. ID가 곧 파일명이므로 파일 목록 탐색 없이 바로 요청하여 로딩 지연이 없다. 오디오 파일은 `audio/{unit_code}/` 폴더에 보관한다.

> **1.4.4 현재 기준**: 오디오 폴더는 `audio/{unit_code}/`이며, URL 구성 기준은 `U` + 행 ID이다.

---

## 교재양식 Textbook Layout

올댓아라빅 인쇄 교재의 페이지 양식. **유닛 1개 = 4종(표현·패턴·드릴·나쓰)의 페이지로 구성된 한 묶음**이며, 모든 페이지는 B5 사이즈로 디자인되어 화면 미리보기 = 인쇄물 1:1 매칭이다. 구현 파일: `textbook144.html`.

### 페이지 사이즈

- **B5** (182mm × 257mm)
- CSS: `width: 182mm; height: 257mm` — 모니터에서 보이는 크기 = 실제 인쇄 크기 (1:1)
- 표현·드릴·나쓰 페이지는 콘텐츠 양에 따라 세로로 늘어날 수 있음 (`min-height: 257mm; overflow: visible`). 패턴 페이지는 고정 B5.

### 디자인 토큰

앱과 공유한다. `appendix_css(ata144).md` 의 `:root` 토큰을 그대로 사용.

```
--accent: #1D49FF   /* 파란 액센트 */
--text:   #12205A   /* 본문 다크블루 */
--sub:    #888FAC   /* 보조 회색 */
--border: #D5DAE8
--bg:     #F2F4F6   /* 회색 영역 배경 */
--gray-pill: #C7CDD9 /* 헤더 우측 박스(회색 알약) */
--font-ar: 'Noto Sans Arabic', serif
--font-kr: 'Spoqa Han Sans Neo', 'Apple SD Gothic Neo', sans-serif
--textbook-card-ar-size: 1.18rem    /* 표현·나쓰·디폴트 드릴 카드의 아랍어 공통 크기 */
--textbook-card-trans-size: 0.74rem /* 표현·나쓰·디폴트 드릴 카드의 음가 공통 크기 */
```

회색 영역(헤더·카드영역·푸터)과 흰색 카드(본문/래퍼토리/표현/드릴/나쓰)의 교대 구성으로 시각적 리듬을 만든다.

인쇄용 B5 교재의 기본 카드형 콘텐츠는 표현(`.exp-arabic`), 나쓰(`.nass-arabic`), 디폴트 드릴(`.drill-arabic`)의 아랍어 본문 크기를 `--textbook-card-ar-size`로 통일한다. 음가 라인은 `--textbook-card-trans-size`를 공통 사용한다. `css = 'five'`는 좁은 5열 셀을 위한 별도 예외값이므로 이 공통 토큰에 연동하지 않는다.

#### 폰트 패밀리 / Weight 규칙

**폰트 패밀리**
- 아랍어: `Noto Sans Arabic`, fallback `serif` (`--font-ar`)
- 한국어/Latin: `Spoqa Han Sans Neo`, `Apple SD Gothic Neo`, sans-serif (`--font-kr`)

**아랍어 weight (페이지/요소별)**

| 요소 | 셀렉터 | weight | 비고 |
|---|---|---|---|
| 패턴 메인 데이터 | `.pattern-arabic` | **700** | 가장 강조 — 학습 대상 패턴 |
| 패턴 알파벳 큰 글자 | `.pattern-arabic.baseline .blch .l` | **700** | baseline 첫 행 (큰 단일 글자) |
| 패턴 알파벳 연결형 | `.pattern-arabic.baseline .blch .l-triple` | **500** | baseline 두 번째 행 (3-글자 연결형) |
| 표현 카드 | `.exp-arabic` | **500** | 카드형 본문 |
| 나쓰 카드 | `.nass-arabic` | **500** | 카드형 본문 |
| 드릴 카드 (non-five) | `.drill-arabic` | **500** | 카드형 본문 |
| 드릴 5열 셀 | `.drill-five td.ar` | **500** | 좁은 셀, 가벼운 weight |
| 래퍼토리 셀 | `.repertory td.ar` | **500** | 셀형 |
| 패턴 스크립트 인라인 토큰 | `.pattern-script .ar-tok` | 600 | 한국어 본문 안 인라인 단어 |

**원칙**
- 패턴(학습 대상 데이터)은 **700**으로 가장 강조
- 그 외 본문/예문/드릴/나쓰는 **500** (한 단계 가볍게)
- 스크립트 안 인라인 아랍어 토큰만 600 — 한국어 본문 사이에서 읽힘 보장

**hl-text (`*...*` 색 강조) weight 규칙**

⚠️ **중요**: `.hl-text`에 부모와 다른 `font-weight`를 지정하면 Safari/CoreText에서 weight 경계로 텍스트 런이 분리되어 **Arabic shaping(글자 연결)이 끊긴다**. 따라서:

| 컨텍스트 | weight 처리 |
|---|---|
| 아랍어 (`.ar-text`, `.pattern-arabic`, `.drill-arabic`, `.nass-arabic`, `.exp-arabic`) | **부모 weight 상속** (지정 안 함). 색만 다르게 (`var(--hl-text)`) |
| 라틴 음가 (`.trans`, `.drill-trans`, `.exp-trans`, `.nass-trans`, `.pattern-trans`) | **font-weight: 700** + 색. Latin은 셰이핑 없으니 bold OK |
| 한국어 (`.pattern-korean`) | **font-weight: 700** + 색. 한국어도 셰이핑 영향 없음 |

```css
.hl-text { color: var(--hl-text); }  /* 기본 — weight 미지정 */
.trans .hl-text,
.drill-trans .hl-text,
.exp-trans .hl-text,
.nass-trans .hl-text,
.pattern-trans .hl-text { font-weight: 700; }
.pattern-korean .hl-text { color: var(--hl-text); font-weight: 700; }
```

**한국어/음가 weight (참고)**
- 음가(trans/drill-trans/exp-trans/nass-trans 등) 본문: **400** (italic)
- 한국어 본문(`.exp-korean`, 카드 한국어 등): **500**
- 한국어 강조(`.pattern-korean` 패턴 한국어): **600**

### 유닛 한 단위의 페이지 구성

각 유닛은 다음 순서로 페이지가 렌더된다:

| 순서 | 페이지 종류 | 분량 |
|---|---|---|
| 1 | 표현 (EXPRESSION) | 1장 |
| 2~ | 패턴 (PATTERN 001, 002, …) | 패턴 수만큼 |
| 마지막-1 | 드릴 (DRILL — drillA + drillB 한 페이지) | 1장 |
| 마지막 | 나쓰 (NASS — nass·nass+ 구분없이 연속, 카드별 B5 자동분할) | 1장+ |

예: A01 (패턴 3개) → **6 페이지** (표현 1 + 패턴 3 + 드릴 1 + 나쓰 1).

페이지 번호는 유닛 안에서 01부터 시작.

---

### 모든 페이지 공통

#### 헤더 (회색 영역)

위에서 아래로:

1. **카테고리 라벨** (작은 회색 텍스트)
   - 표현 페이지: `올댓아라빅 표현`
   - 패턴 페이지: `올댓아라빅 패턴`
   - 드릴 페이지: `올댓아라빅 드릴`
   - 나쓰 페이지: `올댓아라빅 지문`
2. **분할 박스** (좌 파란 / 우 회색 알약)
   - 좌: `UNIT` 라벨(작게) + 유닛 번호(크게). 유닛 번호 = `u`에서 숫자만 추출 (A01 → 1)
   - 우: 페이지 종류에 따라 다름
     - 패턴 페이지: `PATTERN` 라벨(작게) + 패턴 번호(크게, 3자리). 패턴 번호 = `id_ptn(no)`에서 숫자 추출 (ptn001_… → 001)
     - 표현 페이지: `EXPRESSION` 단어 (소형, 숫자 없음)
     - 드릴 페이지: `Drill` 단어 (소형, 숫자 없음)
     - 나쓰 페이지: `NASS` 단어 (소형, 숫자 없음)
3. (드릴/나쓰 한정) **안내문** — 학습 지시문장 (예: "다음을 듣고 따라 읽어 보세요"). 박스 아래 18px 여백 후 콘텐츠 시작.

#### 푸터 (회색 영역, 좌우 분할)

- 좌측: `assets/ata-logo2.png` 로고 + **MARKAZARABIC** + `© 2026 all rights reserved`
- 우측: `| NN` 페이지 번호 (유닛 내부 인덱스)

---

### 페이지 종류별 상세

#### 표현 페이지

**구성**: 헤더 → (그림 있으면 16:9 일러스트) → 표현 카드 N개 → 푸터.

- **표현 카드** (흰색 박스, 카드당 1 표현)
  - 2열 그리드: **좌측 한국어 (LTR) / 우측 아랍어 + 음가 (RTL)**
  - 우측: 큰 아랍어 → 작은 회색 italic 음가
  - 좌측: 한국어 (아랍어보다 작게)
  - `expression.script` 값이 있으면 카드 하단에 가로 구분선 + 좌측 정렬로 추가 표시
  - 라하 깃발: 아랍어 옆 인라인 (해당 페이지 깃발 위치 규칙 참고 ↓)

#### 패턴 페이지 (한 패턴 = 한 페이지)

**구성**: 헤더 → (첫 패턴이면 16:9 일러스트) → 본문 카드 → 래퍼토리 카드 → 푸터.

- **본문 카드** (흰색 박스, 패턴 자체 표시)
  - 일반 패턴: 큰 아랍어 (우측 정렬, RTL) → 음가 (우측, 챕터 0 한정) → 한국어 (가운데) → 스크립트 (좌측)
  - **baseline 패턴** (`pattern.css = 'baseline'`): 글자별 그리드
    - 가운데 정렬, 우→좌 흐름 (`direction: rtl`)
    - 글자 아래에 음가 라벨 (plain text, font-weight 400)
    - 글자 baseline 위치에 가로 기준선 — JS probe로 첫 글자의 baseline 측정해서 동적 위치
  - **baseline2 패턴** (`pattern.css = 'baseline2'`): baseline과 완전 동일한 표 + **가로 기준선만 미표시** (사양 [§8.2.5](product-ata144-design.md))
  - 스크립트 시맨틱 마크업: 단락은 `<p>`, 불릿(`-`로 시작)은 `<ul><li>`로 변환. 인라인 아랍어 토큰은 `.ar-tok` 클래스로 폰트/방향 자동 처리

- **래퍼토리 카드** (흰색 박스, 본문 카드와 가로 폭 동일)
  - 표 자체에 `direction: rtl` — 첫 DOM 셀이 시각적으로 가장 오른쪽(아이템 1번이 우측 시작)
  - 모드 결정:
    - 항목 중 하나라도 `repertory.css = 'five'` → **5열 아랍어 모드** (한국어 무시, 챕터 0이면 음가 작게)
    - 한국어가 모두 비어 있음 → 5열 아랍어 모드 (fallback)
    - 그 외 → **2 페어 모드** (DOM: ar–ko–ar–ko, 시각상 [한국어 아랍어][한국어 아랍어] 우→좌)
  - 라하 깃발: 아랍어 옆 인라인

#### 드릴 페이지

**구성**: 헤더 → DrillA 안내문 + 표(들) → DrillB 안내문 + 표(들) → 푸터. 위→아래 스택. 한 유닛의 드릴이 B5 한 페이지에 다 안 들어가면 박스 단위로 자동 페이지 분할 (아래 "드릴 페이지 분할" 참고).

- **css = 'five'** (5열 모드):
  1. 안내문: `다음을 듣고 따라 읽어 보세요` (가운데)
  2. **DrillA 5열 표** — drillA 항목들이 한 줄에 5개씩
  3. 안내문: `다음을 듣고 빈 칸에 들어갈 말을 유추해 보세요` (가운데)
  4. **DrillB 5열 표** — drillB 항목들이 한 줄에 5개씩. `*X*` 마스크는 회색 음영 박스로 치환

- **css 비어 있음 (디폴트)** — 항목별 카드 (한 드릴 = 한 박스):
  1. 각 드릴 항목이 **자기 흰 카드**(`.drill-row`)를 가짐
  2. 카드 안: 2열 그리드 — **좌측 한국어 (LTR, 보조 톤) / 우측 아랍어 + 음가 (RTL, 학습 대상)**
  3. **수직 정렬: top-align** (`align-items: start`) — 한국어가 아랍어의 윗줄과 같은 라인에서 시작 (나쓰 카드와 동일 방식). 한국어가 길어 다음 줄로 넘어갈 때도 윗부분 라인은 일치
  4. 같은 ref_ptn 그룹 내 카드 사이: **12px 간격** (`.drill-row-gap`) — 나쓰 카드 사이(`.nass-section { gap: 12px }`)와 동일한 호흡으로 통일. 카드 단위 식별성과 그룹 내 흐름의 균형 (모바일에서 스크롤 시 행 단위 하이라이트가 자연스러운 기반)
  5. 서로 다른 ref_ptn 그룹 사이: **14px 간격** (`.drill-box-gap`) — 그룹 분리 강조
  6. 카드 외곽: 둥근 모서리 + 테두리 `1px solid rgba(18,32,90,0.06)` + 옅은 그림자 `0 1px 2px rgba(18,32,90,0.04)`, padding `12px 18px`
  7. 한국어 폰트: `.pattern-script` 본문과 동일 톤 — `0.82rem`, color `var(--sub)`(옅은 회색), line-height `1.5`
  8. 아랍어 `1.08rem` weight 500 / 음가 `0.68rem` italic
  9. 페이지네이션 atom 단위는 ref_ptn 그룹 — 한 그룹의 카드들은 같이 페이지 이동 (atom HTML이 `<카드><gap><카드><gap>...` 형태로 묶여 있음)
  10. (예정) 모바일에서 스크롤 시 행(카드) 단위 도드라지는 하이라이트 처리 — 추후 추가

- **안내문 (instruction) 양식**
  - 안내문 멘트와 챕터별 override는 본 문서 하단 **참고용(이슈) → 안내문 멘트 일람** 표 참조
  - 각 섹션의 안내문은 **첫 등장(첫 박스 위)에만 1회**만 표시. 같은 섹션이 여러 박스(ref_ptn 그룹)로 나뉘어도 안내문은 처음 한 번뿐
  - 페이지가 분할되어 같은 섹션이 다음 페이지로 이어질 때도 안내문 반복하지 않음 — 단, **첫 박스가 다음 페이지로 밀리는 경우 안내문은 첫 박스와 함께 다음 페이지로 이동** (atomic). 안내문이 박스 없이 페이지 끝에 외롭게 남는 케이스 방지
  - 스타일: `.drill-instruction` (가운데 정렬, 본문 색, 0.86rem 정도)
  - **안내문과 첫 박스 사이 간격**: `.drill-instruction { margin-bottom: 28px }`. 부모 `.drill-section { gap: 0 }` 으로 둬서 모든 자식 간 간격은 명시 div(`.drill-row-gap`, `.drill-box-gap`) 또는 margin으로만 제어. flex gap이 자동으로 더해지면 의도한 간격(2px, 14px 등)이 12px씩 부풀려지는 버그를 방지

- **드릴 그룹핑 (ref_ptn)** — 한 유닛의 드릴을 박스 단위로 분리하는 규칙
  - **디폴트**: drill 행에 `ref_ptn` 값이 비어 있으면 해당 유닛 전체를 한 박스에 모아서 표시 (전 유닛을 포괄하는 일반 드릴)
  - **패턴별 분리**: 특정 패턴만 드릴하고 싶을 때 행에 `ref_ptn` 값을 지정 (래퍼토리와 같은 논리). 동일 type(drillA/B) 안에서 `ref_ptn` 값이 다른 그룹들이 있으면 각 그룹마다 **별도의 표를 새로 생성**해서 위→아래로 쌓음. 빈 ref_ptn(유닛 공통) 그룹은 가장 마지막에 표시
  - 같은 type 안에서 박스끼리는 14px 간격(`.drill-box-gap`)으로 분리
  - 시트의 `ref_unit` = 유닛 매칭 키(필수, 자동 적용), `ref_ptn` = 옵션. 두 컬럼은 서로 독립적으로 사용

- **드릴 페이지 분할 (자동, B5 budget 기반)** — `renderDrillPagesAsync`
  - 박스(=ref_ptn 그룹) 1개가 atomic 단위. 박스 중간에서 절대 자르지 않음
  - off-screen 측정으로 각 박스의 실제 높이를 잰 뒤, B5 페이지의 `.page-cards` 가용 높이 안에서 빈 패킹
  - 안 들어가는 박스는 다음 `.page page-drill`로 밀어서 새 페이지 생성
  - 같은 섹션 박스끼리 14px gap, DrillA→DrillB 섹션 전환은 36px gap (`.page-cards { gap }`)
  - **단일 박스가 페이지 가용 높이를 단독으로 초과하는 경우**: 콘솔 경고 (`[drill-pagination] …`) + 그 박스만 한 페이지에 단독 배치. 시트에서 ref_ptn을 더 잘게 쪼개거나 행 수를 줄여 해결
  - 페이지 번호: 드릴이 N페이지로 늘어나면 후속 페이지(나쓰 등)의 번호가 자동으로 N-1만큼 밀림
  - **측정 안정성**: ① 측정 전 `document.fonts.ready` 대기 — 폰트 로딩 전 측정하면 fallback 폰트 메트릭이 나와 브라우저별 mismatch 발생. ② `PAGE_BUDGET`은 측정값이 아닌 **B5 고정 기하로 하드코딩 (740px)** — 측정 컨테이너의 flex 레이아웃이 브라우저별로 다르게 계산되는 이슈 회피 (Chrome ↔ Safari)

- **DrillB 빈칸 처리**
  - `*X*` → 회색 음영 박스(`<span class="drill-blank">`)로 치환. 밑줄 X.
  - 아랍어 셀: 빈칸 양옆에 `ـ` (tatweel) 자동 삽입 — **단** 마스크가 단어 시작/끝일 때 그쪽 tatweel 생략
  - 라틴 음가: tatweel 없이 박스만
  - 시트 데이터 변환 없이 렌더링 시점 자동 처리

#### 나쓰(지문) 페이지

**구성 (2026-05-20 개정)**: 헤더 → 안내문 1개 → `nass`·`nass+` **구분 없이** 시트 순서대로 카드 연속 → 푸터.

- **안내문 (nass·nass+ 공통, 1개)**: `다음을 들으면서 소리로 의미를 이해하고 같이 따라 읽어 보세요`
- `nass`/`nass+`는 시트 순서대로 한 흐름으로 표시한다(섹션 분리·별도 안내문 없음).
- **카드별 B5 자동 분할** (`renderNassPagesAsync`): 카드 1장이 페이지를 넘으면 그 카드부터 다음 페이지로. 드릴과 동일한 B5 기하(PAGE_BUDGET 740px), 카드 간 12px(`.nass-section gap`). 안내문은 **첫 페이지에만**, 페이지 번호는 연속.
- 교재앱(`ata144_textbook`)도 동일하게 nass·nass+ 한 안내문으로 연속(스크롤). page 컬럼 분리는 nass에 미적용.
- **나쓰 카드** (흰색 박스)
  - 2열 그리드: **좌측 한국어 (LTR) / 우측 아랍어 + 음가 (RTL)**
  - 글자 크기는 표현 카드보다 살짝 작게 (대화 2~3줄 들어가도 답답하지 않게). line-height 여유.
  - 데이터의 `\n` 줄바꿈은 `<br>`로 자동 변환되어 화자 분리 표시
  - 라하 깃발: 한국어 칸 **하단**에 표시 (페이지별 깃발 위치 규칙 참고 ↓)

---

### 일러스트 (GCS 호스팅)

**노출 위치 (임시 규칙)**

- **표현 페이지**: 헤더 바로 아래, 첫 표현 카드 위
- **첫 패턴 페이지**: 헤더 아래, 본문 카드 위 (한 유닛에서 가장 첫 패턴 페이지에만)
- 두 위치 모두 16:9 비율, 박스/라운드 없는 직사각형으로 회색 페이지 배경 위에 배치
- 다른 페이지(드릴, 나쓰)에는 일러스트 없음

**파일명 컨벤션**

| 위치 | 파일명 패턴 | 예 |
|---|---|---|
| 표현 페이지 | `img/{유닛U}_exp1.png` | `A01_exp1.png` |
| 첫 패턴 페이지 | `img/{유닛U}_ptn1.png` | `A01_ptn1.png` |

URL 풀패스: `https://storage.googleapis.com/all-that-arabic-14/img/{파일명}`

- 시트의 `img` 컬럼은 사용 안 함 — 유닛 U값에서 자동 생성
- 접미사 `_exp1` / `_ptn1`의 `1`은 추후 한 유닛에 여러 이미지(베리에이션)를 둘 가능성을 위한 자리. 현재는 1번만 사용. 베리에이션 방식은 추후 결정.

**없을 때 처리**

- `<img>` 로드 실패(404) 시 `onerror`로 일러스트 영역을 DOM에서 제거 — placeholder 표시 없이 다음 콘텐츠가 자연스럽게 위로 올라옴
- GCS에 파일을 올리거나 빼는 것만으로 자동 반영 (시트/코드 수정 불필요)
- 파일은 GCS에서 공개 읽기 권한 필요

**캐시 무력화** (초안 단계)

`<img src="…?v=Date.now()">` — 매 페이지 로드마다 새 timestamp로 GCS 캐시(max-age=3600) 우회. 배포 시엔 etag/버전 기반 등으로 변경.

---

### 라하(방언) 국기

**코드 → 국기 매핑**

| 코드 | 국기 |
|---|---|
| `EGY` / `EGP` | 이집트 (`assets/icons/icon-egypt.png`) |
| `LEV` | 레바논 (`assets/icons/icon-lebanon.png`) |
| `GLF` | 사우디 (`assets/icons/icon-saudi.png`) |
| `MSA` 등 매핑 없는 값 | 미표시 |

**다중 라하 지원**

`lahja` 컬럼에 `GLF, LEV` 또는 `GLF LEV` 같이 콤마/공백으로 두 개 이상 입력하면 두 국기 모두 표시 (16×16 원형, 4px 간격으로 나란히).

**페이지별 깃발 위치**

| 페이지 | 깃발 위치 | 깃발 크기 |
|---|---|---|
| 표현 카드 | 우측 아랍어 옆 인라인 | 14×14 |
| 래퍼토리 셀 (페어 모드) | 셀 내 아랍어 옆 인라인 | 14×14 |
| 드릴 카드 (non-five) | 아랍어 옆 인라인 | 14×14 |
| **5열 모드 (`css = 'five'`)** — 드릴 5열 셀 + 래퍼토리 5열 셀 공통 | **아랍어 옆 인라인** (위치 동일) | **10×10** (좁은 셀에서 wrap 방지를 위해 축소) |
| 나쓰 카드 | 좌측 한국어 칸 하단 (특정 줄에 매여 보이지 않도록 카드 메타로 분리) | 14×14 |

**5열 모드 공통 규칙** (`.css-five` 클래스)

`.drill-five` (드릴 5열 컨테이너)와 `.repertory.css-five` (래퍼토리 5열 테이블) 둘 다 마커 클래스 `.css-five`를 부여해 동일한 깃발 처리를 받는다. 단일 CSS 셀렉터(`.css-five .ar-line .lahja-flag`)로 한 군데서 관리:

```css
.css-five .ar-line { gap: 4px; }
.css-five .ar-line .lahja-flag { width: 10px; height: 10px; }
.css-five .ar-line .lahja-flags { gap: 2px; }
.css-five .ar-line .lahja-flags .lahja-flag { width: 10px; height: 10px; }
```

**5열 박스/셀 사이즈 통일** — 드릴 5열과 래퍼토리 5열의 박스/셀이 시각적으로 동일하게 보이도록 다음 값을 통일한다:

| 항목 | 값 | 비고 |
|---|---|---|
| 외곽 박스 패딩 | `14px 16px` | `.card`와 동일 (드릴은 자체 박스, 래퍼토리는 `.card` 안) |
| 셀 패딩 | `7px 6px` | 좁은 5열 셀에 맞춘 타이트한 값 |
| 셀 폰트 크기 | `1.08rem` | 두 곳 동일 |
| 셀 폰트 weight | `500` | 두 곳 동일 |
| 트랜스 폰트 크기 | `0.68rem` | 두 곳 동일 |
| 트랜스 margin-top | `1px` | 두 곳 동일 |
| 트랜스 letter-spacing | `0.2px` | 두 곳 동일 |

---

### 인라인 마크업 처리

| 마크업 | 본문(패턴 자체/한국어/스크립트) | 셀(레퍼토리/드릴/음가) |
|---|---|---|
| `_..._` (박스 강조) | `<span class="hl-box-inline">` 박스 | **제거** (다이아크리틱 클립 회피) |
| `*...*` (색 강조) | `<span class="hl-text">` 색 강조 | `<span class="hl-text">` 색 강조 (유지) |
| `\n` (줄바꿈) | 모든 곳에서 `<br>`로 변환 | (셀에는 보통 등장 안 함) |
| `\|` (수직 막대, `css = 'nom'` 행만) | 명사문 시작어 ‖ 서술어 사이 — `<span class="nom-divider">\|</span>` 옅은 회색 분리자 | (셀에서도 동일 적용) |

박스 강조(`_..._`)가 sukoon/diacritic이 있는 아랍어 글자(예: `بْ`)에서 윗 점/마크를 잘라먹는 문제 때문에 셀에서는 비활성. 색 강조(`*...*`)는 시각적으로 안전해 모든 곳에서 유지.

`|`는 `css = 'nom'` (명사문, الجملة الاسمية) 행에서만 분리자로 치환된다. 다른 행에서는 그대로 표시. 자세한 사양·CSS는 [`product-ata144-design.md`](product-ata144-design.md) §8.3.7 참고.

---

### 챕터 0 (음가 노출 기준)

- `u`가 `A`로 시작하는 유닛 → 챕터 0(알파벳 입문). 음가(`note`/`transliteration` 컬럼) 자동 노출
- URL 파라미터로 강제 가능: `?trans=1` 노출, `?trans=0` 숨김
- 다른 시리즈(예: 추후 G/B 등)에는 음가 자동 노출 안 함

---

### 데이터 소스

1.4.4 시트 (`1w7e0mLLgFhzU7Ixs6CUfzgrwUG6EEy8jHijXF5UJwY8`). 사용 탭:

| 탭 | 역할 | 핵심 컬럼 |
|---|---|---|
| `unit` | 유닛 메타 | `u`, `arabic`, `korean`, `note`(음가) |
| `pattern` | 패턴 본문 | `arabic`, `korean`, `note`(음가), `script`, `css`, `id_ptn(no)` |
| `repertory` | 패턴별 예문 | `arabic`, `korean`, `note`, `css`, `ref_ptn`, `lahja`, `id_drill`, `url` |
| `expression` | 유닛별 표현 | `arabic`, `korean`, `note`, `script`, `lahja`, `id_kalimat`, `url` |
| `drill` | 유닛별 드릴 | `arabic`, `korean`, `note`, `type`(drillA/B/writing), `css`, `ref_unit`(유닛 키), `ref_ptn`(옵션, 박스 분리), `lahja`, `id_drill`, `url` |
| `nass` | 유닛별 지문(대화) | `arabic`, `korean`, `note`, `type`(nass/nass+), `lahja`, `id_nass`, `Img`, `sual`, `jawab` |

**연결 키**

- 패턴 ↔ 레퍼토리: `pattern.id_ptn(no)` ↔ `repertory.ref_ptn`. ref_ptn이 비어 있는 행은 어떤 패턴에도 매칭되지 않아 표시되지 않는다.
  - **매칭 로직**: 1차 strict 매칭 (전체 문자열 동일) → 실패 시 **ptn-번호 prefix만** 추출해서 매칭 (`/^ptn\d+/i` — 예: `ptn001`). 시트에서 두 컬럼의 언더스코어 뒤 설명 부분이 unsync되어도(예: pattern은 `ptn001_ba-ta-tha`, repertory는 `ptn001_bā tā thā`) 자동 매칭되어 래퍼토리가 안 보이는 사고 방지
- 표현·나쓰: 유닛 단위 매칭만 (`ref_unit`)
- **드릴**: 유닛 매칭은 `drill.ref_unit`이 기본 키. `drill.ref_ptn`은 옵션 — 값이 있으면 박스를 패턴 단위로 분리하고, 비어 있으면 유닛 공통 박스에 합쳐서 표시 (위 "드릴 그룹핑" 참고)

**컬럼명 호환**

음가 컬럼은 시트에서 `note`로 통일되어 있다. `fetchSheet`는 `note` ↔ `transliteration` 양쪽 키를 모두 채워서 반환하므로 코드에서는 어느 이름으로 접근해도 동일하게 동작.

**fill-down (drill 탭 전용)**

drill 탭은 섹션 헤더 행에만 `type` / `status`가 명시되고 후속 행은 비어 있는 경우가 있어 `fillDownDrill()`로 직전 비어있지 않은 값으로 자동 채움.

**페치**

`fetchSheet(name)`은 다음 순서로 시도:
1. `fetch()` + CORS — 서버 호스팅 환경 우선
2. JSONP fallback — `file://` 등 CORS 불가 환경 대응

---

### 인쇄 스타일

`@media print` 규칙:
- 배경 단순화, 그림자/라운드 제거
- 유닛 픽커 숨김
- 각 페이지마다 `page-break-after: always`

---

### CSS 코드

#### baseline (알파벳 그룹용)

알파벳 그룹 페이지(A01-A05의 ptn001 등). **6칸 고정 그리드** + 유닛별 글자 크기 일관 + 토큰이 6 미만이면 가운데 정렬. 모바일 이식 시 `1/6` 비례 그대로 적용된다.

데이터 형식 (`pattern.arabic`):
- 1줄: 단일 글자 (`|`로 구분) — 예: `ب | ت | ث | ن | ي`
- 2줄: 위치별 연결형 (`|`로 구분) — 예: `ببب | تتت | ثثث | ننن | ييي`

`note` 컬럼은 음가 라벨 (공백 구분).

렌더 결과 HTML:

```html
<div class="pattern-arabic baseline has-triple">
  <div class="bl-line"></div>
  <span class="blch">
    <span class="l">ب</span>          <!-- 1행: 단일 글자 -->
    <span class="l-triple">ببب</span>  <!-- 2행: 연결형 -->
    <span class="lbl">bā</span>       <!-- 3행: 음가 -->
  </span>
  ... (4~5개 더)
</div>
```

CSS:

```css
/* baseline 모드: 6칸 고정 그리드 (알파벳 그룹 최대 6개)
   - 셀 폭은 컨테이너의 1/6로 고정 → 유닛별 글자 크기 일관
   - 토큰이 6 미만이면 가운데 정렬 (justify-content: center)
   - 모바일 이식 시에도 동일 비례 유지 */
.pattern-arabic.baseline {
  position: relative;
  display: flex;
  direction: rtl;
  justify-content: center;
  flex-wrap: nowrap;
  width: 100%;
  padding: 22px 0 18px;
  text-align: initial;
}

/* 글자 baseline 위치에 가로 기준선 — JS probe로 첫 글자의 baseline 측정해서 동적 위치 */
.pattern-arabic.baseline .bl-line {
  position: absolute;
  left: 4%; right: 4%;
  height: 1.5px;
  background: rgba(29, 73, 255, 0.3);
  pointer-events: none;
  z-index: 0;
}

/* 셀 — 단일 글자 + 연결형 + 음가 라벨 세로 스택 */
.pattern-arabic.baseline .blch {
  flex: 0 0 calc(100% / 6); /* 셀 폭 = 컨테이너의 1/6 (6칸 기준) */
  display: flex; flex-direction: column;
  align-items: center; gap: 14px;
  position: relative; z-index: 1;
  min-width: 0;
}

/* 1행: 큰 단일 글자 (유닛별 동일 크기) */
.pattern-arabic.baseline .blch .l {
  font-family: var(--font-ar); font-weight: 700;
  font-size: 2.1rem; line-height: 1;
}

/* 2행: 3-글자 연결형 (위치별 형태) — 레퍼토리 셀 크기 근처 */
.pattern-arabic.baseline .blch .l-triple {
  font-family: var(--font-ar); font-weight: 600;
  font-size: 0.95rem; line-height: 1.2;
  color: var(--text);
  white-space: nowrap;
  text-align: center;
}

/* 3행: 음가 라벨 (한글 폰트, 작게) */
.pattern-arabic.baseline .blch .lbl {
  font-family: var(--font-kr);
  font-size: 0.72rem; font-weight: 400;
  color: var(--sub-2);
  direction: ltr; letter-spacing: 0.2px;
  white-space: nowrap;
}
```

토큰 분리 로직 (`renderBaseline`):

```js
const splitTokens = (line) => {
  // 데이터에 `|`가 있으면 그걸로 분리, 없으면 공백으로
  // (한 토큰 안에 공백/콤마가 들어가도 보존: "اـ، ـاـ ، ـا" 같은 위치별 형태)
  const sep = line.includes('|') ? /\s*\|\s*/ : /\s+/;
  return line.split(sep).map(t => t.trim()).filter(Boolean);
};
```

기준선(`bl-line`) 위치는 폰트 로드 후 첫 글자(`.l`)의 baseline을 JS probe로 측정해서 `top` 으로 동적 설정 (`adjustBaselines()`). 슬라이드 앱(`app144.html`)과 동일한 방식.

---

### 향후 계획 / 미정

- **오디오 재생 인터랙션**: 교재용 앱에서는 아랍어 텍스트 클릭/탭으로 재생한다. 선생님용 앱에서는 재생 버튼 또는 `S` 키로 재생한다.
  - 시각적 재생 버튼 UI는 교재용 앱에는 두지 않음 (아랍어 텍스트 자체가 인터랙션 대상)
  - 오디오 URL: 시트의 `url` 컬럼 우선, 없으면 `https://storage.googleapis.com/all-that-arabic-14/audio/{unit_code}/{id}.mp3`
- **이미지 베리에이션**: `_exp1`/`_ptn1` 의 `1` 자리를 활용한 베리에이션(여러 이미지) 방식 추후 결정
- **모바일 대응**: 현재는 데스크톱/인쇄용 고정 B5. 모바일은 별도 양식 또는 반응형 처리 필요
- **학생용 앱 통합 가능성**: 현재의 `app144s.html` 학생용 앱을 본 교재 양식으로 통일/대체 검토 중 (미정)
- **학생용 교재 앱**: 인쇄용 B5 교재와 모바일 교재 앱은 같은 반응형 값을 공유하지 않는다. B5 출력용 값은 `textbook144.html` 기준으로 유지하고, 학생용 교재 앱은 별도 디자인 코드로 정의한다.

---

## 참고용 (이슈)

> 진행 중 발견된 시각·레이아웃 이슈를 모아두는 섹션. 본문 규칙으로 흡수되기 전까지 여기에 기록.

### 라하(방언) 국기 위치

| # | 이슈 | 페이지 | 처리 | 비고 |
|---|---|---|---|---|
| 1 | 5열 모드 셀에서 깃발-아랍어 충돌 / 좁은 셀에서 텍스트 줄바꿈 | 드릴 + 래퍼토리 (`css = 'five'`) | **(최종)** 5열 모드 컨테이너에 공통 마커 `.css-five` 부여. 깃발은 인라인 위치 유지하되 **10×10으로 축소** + `.ar-line { gap: 4px }`. 텍스트 폭 손실 ~12px → 좁은 셀에서도 wrap 안 남. **(이전 시도)** ① 인라인 6px gap (14px 깃발) → 다이아크리틱과 충돌 ② gap 10px (14px 깃발) → 짧지 않은 단어에서 줄바꿈 ③ 좌상단 코너 절대 배치 → 헤더처럼 보여 부자연스럽고 드릴에만 적용된 비공통 처리라 폐기 | 깃발 식별성은 다소 양보(10px). 추후 더 작아지면 색만 보이는 점으로도 충분할 가능성 검토 |
| 2 | 나쓰 카드에서 깃발이 아랍어 옆 인라인이면 두 번째 줄(다른 화자)에만 방언 적용된 것처럼 보이는 오해 발생 | 나쓰 | 깃발을 좌측 한국어 칸 **하단**으로 분리 (현재 운영 중인 처리) | 본문 규칙에 이미 반영됨 (페이지별 깃발 위치 표). 잠정적으로는 정착으로 간주 |
| 3 | 표현/래퍼토리 등 일반 셀에서 다중 라하(`GLF, LEV`) 표시 시 깃발이 두 개라 가로 폭을 더 잡아먹음 | 표현·래퍼토리·드릴 | 16×16 → 14×14로 축소 + 깃발 사이 4px 간격 (현재 운영 중) | 추가 축소 시 식별성 저하 위험. 모바일에선 별도 처리 검토 |

### 추가 후보 (관찰 중)

- (예정) 다이아크리틱 위쪽으로 튀어나오는 깃발 충돌 (마지막 글자 위에 ḍamma + 인접 깃발)
- (예정) 음가/한국어 라인이 깃발 위치에 따라 들쑥날쑥하게 정렬되는 케이스

### 드릴 페이지 분할 / 안내문 동행 규칙

| # | 이슈 | 페이지 | 임시 처리 | 비고 |
|---|---|---|---|---|
| 4 | 한 유닛의 드릴 박스 합이 B5 한 페이지를 넘어 푸터/마지막 행이 잘림 (예: 유닛 2 → DrillA 3박스 + DrillB 6카드, ~940px > 가용 ~770px) | 드릴 | `renderDrillPagesAsync` 도입 — 박스 단위 atomic + off-screen 측정 + 빈 패킹으로 페이지 자동 분할. `.page-drill { height: auto; min-height: 257mm; overflow: visible; }` 오버라이드 제거 (B5 고정으로 회귀) | 단일 박스가 페이지보다 큰 극단 케이스만 콘솔 경고 + 단독 페이지 배치. 추후 행 단위 분할 fallback 검토 |
| 4-1 | 페이지 분할 결과가 브라우저별로 다름 — Chrome에선 2페이지(정상)인데 Safari에선 박스마다 한 페이지로 잘게 분할 | 드릴 | ① 측정 전 `document.fonts.ready` 대기 추가 (폰트 로딩 전 측정하면 fallback 메트릭으로 잘못 계산) ② `PAGE_BUDGET`을 측정값이 아닌 **B5 기하 기반 하드코딩 (740px)** 으로 변경 — 측정 컨테이너의 flex 레이아웃이 Safari에서 다르게 계산되어 budget이 비정상적으로 작게 잡히던 문제 해결 | 하드코딩 값은 page-head 130 + page-foot 50 + page-cards padding 40 차감(971-220=751)에 안전 마진 11px 적용. 페이지 헤더/푸터 디자인 바뀌면 이 값 재산출 필요 |
| 5 | 안내문이 박스 없이 페이지 끝에 외롭게 남거나, 같은 섹션이 페이지를 넘어갈 때 안내문이 두 번 반복되는 어색함 | 드릴 | 안내문은 **각 섹션 첫 등장의 첫 박스에만** 부착(atomic). 첫 박스가 다음 페이지로 밀리면 안내문도 같이 이동. 같은 섹션의 후속 박스/후속 페이지에는 안내문 없음 | 안내문 텍스트 양식은 본문 "안내문 (instruction) 양식" 절에 정리 |
| 5-1 | 안내문이 박스에 너무 붙어 있어 박스 헤더처럼 읽혀 시각적 위계가 흐트러짐 (드릴 + 나쓰 공통) | 드릴, 나쓰 | `.drill-instruction` / `.nass-instruction`의 `margin-bottom`을 6px → **16px**로 늘림. 부모 섹션의 `gap: 12px`와 합쳐 총 ~28px 호흡 공간 확보 | 안내문은 섹션 헤더 성격이라 박스에서 충분히 분리되어야 함. 박스 사이 간격(`.drill-box-gap` 14px)보다 안내문↔박스 간격이 크게 잡혀야 위계가 살아남 |
| 5-2 | 디폴트 드릴 카드 사이 의도한 2px 간격이 실제로는 ~26px로 보였음 — 부모 `.drill-section { gap: 12px }`가 모든 직계 flex 자식 사이마다 12px을 자동으로 추가해서 `.drill-row-gap`(2px) 양옆에도 12px씩 더해진 결과 (`12+2+12=26`). 그룹 간 `.drill-box-gap`(14px)도 같은 이유로 38px이 됨 | 드릴 | `.drill-section { gap: 12px }` → `gap: 0` 으로 변경. 모든 자식 간 간격은 **오직 명시 div**(`.drill-row-gap`, `.drill-box-gap`) 또는 **margin** 으로만 제어. 보상으로 `.drill-instruction { margin-bottom: 16px → 28px }` (부모 gap 12px 흡수) | 결과: 카드 간 정확히 2px, 그룹 간 정확히 14px, 안내문 아래 정확히 28px. 향후 .nass-section은 별도 구조(parent gap 사용)라 손대지 않음 — 드릴과 나쓰의 간격 모델은 다르게 운영됨 |
| 6 | (예정) 콘텐츠가 적은 유닛에서 드릴 페이지 하단에 큰 여백이 남음 — 표현/나쓰 페이지로 흘려 채울지 여부 미정 | 드릴/표현/나쓰 | 미처리 — 일단 페이지 단위 분리 유지 | 학생용 인쇄물 일관성 vs 종이 절약 트레이드오프 |
| 7 | DrillB 5열 모드에서 `*X*` 빈칸 처리 시 글자를 `&nbsp;` + tatweel(ـ)로 치환하니 아랍어 셰이핑이 끊겨 단어가 깨져 보임 (예: `بَ ـ ـت` 식으로 분리) | 드릴 B (5열 모드) | **글자를 보존 + 색만 배경과 동일하게 숨김** (`.drill-blank { color: rgba(18,32,90,0.08); background-color: rgba(18,32,90,0.08); display: inline }`). ⚠️ `display: inline-block`을 쓰면 새 formatting context가 생겨 inline-block 안에서 셰이핑이 끊김 — **반드시 `display: inline` 유지**. 박스 폭은 padding(`0 3px`)과 정답 글자 길이로 자동 조정 | 부수 효과 ① 학생이 빈칸 위에 연필로 답을 쓸 수 있음 ② 채점/해답지에선 `.drill-blank { color: var(--text) }` 한 줄로 정답 노출 ③ tatweel 임의 삽입 로직 제거 — 원래 글자가 그대로 있어 연결 자연. 트랜스(음가) 컨텍스트는 `.trans .drill-blank` + `.drill-trans .drill-blank` 둘 다 매칭해 살짝 더 옅은 톤(`0.06 alpha`) 적용 |

### 안내문 멘트 일람

본 표는 본문 "안내문 (instruction) 양식" 절의 디폴트 멘트 + 챕터별 override를 한 곳에 정리.

**디폴트 멘트** (모든 챕터 공통, 다른 override가 없을 때 사용)

| 페이지 | 섹션 | 멘트 |
|---|---|---|
| 드릴 | DrillA (첫 박스 위) | `다음을 듣고 따라 읽어 보세요` |
| 드릴 | DrillB (첫 박스 위) | `다음을 듣고 빈 칸에 들어갈 말을 유추해 보세요` |
| 나쓰 | `nass`·`nass+` 공통 (1개) | `다음을 들으면서 소리로 의미를 이해하고 같이 따라 읽어 보세요` |
| 연습 | `practice` (양식 미정 — 아래 이슈) | `다음을 듣고 마지막에 올 말을 유추해 보세요` |

**챕터별 override**

| 챕터 / 유닛 | 적용 페이지·섹션 | override 멘트 | 사유 |
|---|---|---|---|
| 챕터 0 (알파벳) — 유닛 **A01~A06** | 드릴 DrillB | `다음을 듣고 빈 칸에 들어갈 **글자**를 유추해 보세요` (`말` → `글자`) | 챕터 0의 학습 단위는 단어가 아닌 글자(자음/모음). 학생이 "어떤 글자가 빠졌는지" 추측하는 작업이라 정확한 표현 |

**구현 위치**: `textbook144.html` 상단 상수 + 헬퍼 함수
- `DRILL_INSTR_A`, `DRILL_INSTR_B_DEFAULT`, `DRILL_INSTR_B_CHAPTER0` 상수
- `_drillInstrBFor(unitU)` — `A0[1-6]` 정규식 매치로 챕터 0 판단
- 새 챕터별 변형 추가 시: 새 상수 정의 + `_drillInstrBFor` (또는 유사 헬퍼) 분기 추가

### 폰트 크기 이슈

| # | 이슈 | 페이지 | 임시 처리 | 비고 |
|---|---|---|---|---|
| 7 | 표현·나쓰·디폴트 드릴 카드의 아랍어 크기가 서로 달라 같은 카드형 콘텐츠인데 위계가 흔들림 (`exp=1.18rem`, `nass=1.05rem`, `drill=1.32rem`) | 표현·나쓰·드릴 카드 모드 | B5 출력용 공통 토큰 도입: `--textbook-card-ar-size: 1.18rem`, `--textbook-card-trans-size: 0.74rem`. `.exp-arabic`, `.nass-arabic`, `.drill-arabic` 및 각 음가 라인이 이 값을 사용 | `css = 'five'`는 5열 좁은 셀을 위한 개별 css 속성이므로 기존 별도 크기 유지. 학생용 교재 앱은 별도 디자인 코드에서 재정의 |

### 드릴 `page` 열 — 한 화면 묶음 명시 (구현됨, 일반원칙 확정 대기)

> **상태**: 2026-05-18 교재앱·선생님앱 **구현 완료** (배포됨). 1·2과는 `page` 미입력이라 기존 방식 유지. 3과(A03)·4과(A04) 시범 적용 중. 일반 원칙(이전 유닛 소급 적용 등)은 검증 후 확정 예정.
>
> **구현 요약**: drill 행 `page` 값이 같은 유닛 내 동일 = 한 화면. 교재앱은 page별 스와이프 페이지 분리, 선생님앱은 page 오름차순 슬라이드 정렬(같은 page는 시트순). `page` 없는 유닛은 기존 ref_ptn 로직. arabic 빈 행은 수집 제외(유령 페이지 방지). 디자인코드 §4.6 참조.

**배경**: 현재 드릴 묶음/분할은 두 단계 자동 로직 ─ ① `ref_ptn` 박스 분리 ② `renderDrillPagesAsync`의 B5 실측 자동 페이지 분할. 작가가 "한 화면(=한 페이지)에 어떤 드릴을 묶을지"를 직접 통제할 수 없다.

**변경**: `drill` 탭에 **`page` 열 신설**. 같은 유닛 안에서 `page` 값이 같은 드릴 행 = **한 화면에 함께 보이는 묶음**. 교재(인쇄 B5)와 교재앱(모바일) 양쪽에 동일 적용.

**선생님앱 연계 (예정, 미구현)**: `page` 묶음을 상단 네비게이션에 `드릴1 / 드릴2 …` 탭으로 노출. (현재 선생님앱은 `drillA / drillB` 섹션 탭 + 슬라이드 1행/장. page 묶음 단위 네비로 전환 검토.)

**1·2과 기준선 (그대로 둠)**: `ref_ptn` 그룹핑 + B5 자동 측정 분할(`renderDrillPagesAsync`, PAGE_BUDGET 740px 하드코딩). 본문 "드릴 그룹핑 (ref_ptn)" / "드릴 페이지 분할" 절 참조.

**확정 필요 (3과 적용 전 결정할 것)**:

| # | 쟁점 | 옵션 |
|---|---|---|
| a | `page` 값 형식 | `1,2,3…` 정수(유닛마다 1부터) / `p1,p2` / 기타 |
| b | 빈 `page` 행 처리 | 기존 자동 분할로 폴백 / 전체 1페이지 취급 / 노출 제외 |
| c | 자동 B5 분할과의 관계 | `page`가 자동 분할을 **완전 대체**(작가 통제 절대) / page 묶음이 B5 초과 시엔 그 안에서만 자동 재분할(인쇄 한정) |
| d | `ref_ptn`과 공존 | `page` 도입 후 `ref_ptn` 박스 분리 유지 / `page`가 상위 개념으로 흡수 |
| e | 교재 vs 교재앱 | 같은 `page` = 인쇄 한 B5면 = 앱 한 스와이프 화면 (1:1) 맞나 |
| f | 적용 범위 | drill 탭 한정 / 추후 exp·nass 등 확장 여부 |

### `practice` 타입 — 양식 미정 (2026-05-20)

> **상태**: 안내문만 확정, **표시 양식 미정**(추후 결정).

- 신규 타입 `practice`. 안내문 = **`다음을 듣고 마지막에 올 말을 유추해 보세요`** (확정).
- `nass`/`nass+`와 별개. nass·nass+는 이제 구분 없이 한 안내문(따라 읽기)으로 통합됐고, "유추" 활동은 `practice`로 분리될 예정.
- **미정 사항**: `practice`의 데이터 양식(어느 탭/컬럼), 카드/렌더 형태, 빈칸·정답 표시 방식, 교재·교재앱·선생님앱 노출 규칙. 정해지면 본문 규칙으로 승격.
- 현재 코드: 미구현(렌더 분기 없음). 시트에 `practice` 행이 있어도 표시되지 않음.

### 패턴 hero 이미지 — 데이터 대체 표시 (임시, 2026-05-20)

> **상태**: 임시 원칙. A07 ptn019부터 시범 적용 — 향후 일반화 여부·간격값·이미지 비율 가이드 결정 후 본문 규칙으로 승격.

- **트리거**: 패턴 행의 `id_ptn(no)` 와 동일한 파일명이 GCS `img/` 에 존재(`{id}.{png|webp|jpg|jpeg}`).
- **표시 규칙**: 이미지가 패턴 박스 **내부 최상단**에 카드 가로폭 가득(좌우 패딩 0, 상단 패딩 0)으로 표시.
- **데이터 생략**: 이미지가 성공적으로 로드되면 패턴 카드의 **아랍어/한국어/음가는 숨김** (이미지가 그 정보를 대체). 스크립트만 18px 간격으로 노출.
- **안전망**: 이미지가 4개 확장자 모두 실패 시 JS 콜백에서 클래스 제거 → 일반 패턴 카드 양식(데이터 노출)으로 자동 복원. 시트에 이미지 표시 컬럼 없음(코드가 자동 판단).
- **적용 범위**: 교재용 앱(`ata144_textbook.html`) 한정. 선생님앱·인쇄 교재는 종전 양식 유지.
- **CSS 키**: `.m-card.pattern.pattern-with-hero` (data 숨김 + 이미지 가로폭 + 스크립트 간격 모두 이 클래스 안에서 처리).

미정 사항: 이미지 가로비/세로비 가이드, 단일 패턴이 아닌 패턴+예문 묶음에서 이미지가 어디까지 영향을 줄지(현재는 패턴 카드만), 스크립트 폰트/간격 추가 조정 여부.

### 섹션 순서 — 7과부터 패턴 우선 (2026-05-20)

> **상태**: 본문 규칙. 1~6과는 알파벳 학습기로 표현 우선, 7과부터 본격 문법 학습에 들어가며 패턴 우선으로 전환.

**1~6과 (알파벳 학습기)**: 표현 → 패턴 → 드릴 → 지문 → 단어
**7과부터 (문법 학습기)**: **패턴 → 드릴 → 표현 → 지문 → 단어**

- 적용 범위: 교재용 앱(`ata144_textbook.html`). 선생님앱·인쇄 교재는 별도 검토 후 동기화 예정.
- 판정: 유닛 코드의 숫자 부분이 7 이상 (`A07`, `A08`, ...).
- 영향: 페이지 시퀀스, 상단 탭 표시 순서, 첫 페이지 모두 위 순서를 따름.
- 헬퍼: `unitNum(u)`, `isPatternFirst(u)`, `sectionOrderFor(u)`.
