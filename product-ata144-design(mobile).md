# 모바일 앱 실험 — ATA 1.4.4

> **현재 매핑 (2026-05 기준)**
>
> 본 문서가 작성될 당시의 실험용 파일명은 이후 정식 경로로 옮겨졌다. 본문의 옛 이름은 다음과 같이 매핑된다:
>
> | 본문 표기 (옛 이름) | 현재 경로 |
> |---|---|
> | `app144s.html` | `ata144_student/ata144_student.html` |
> | `textbook144.html` (인쇄 교재) | `ata144_original/ata144_original.html` |
> | `textbook144m.html` (모바일 실험) | `ata144_textbook/ata144_textbook.html` |

> **맥락 (Context)**
>
> 올댓아라빅 1.4.4의 학생용 모바일 앱은 현재 `app144s.html` 한 가지가 운영 중이다 (제품 앱). 이 문서는 그 **공식 학생앱과 별개로**, 새로 만든 **교재 양식(`textbook144.html`)을 모바일 인터랙티브 앱으로 옮길 수 있을지** 실험하기 위한 기록이다.
>
> 즉 두 갈래가 동시에 존재하는 상태:
> - **`app144s.html`** — 기존 제품 학생앱 (Vue/슬라이드 기반, 1.4.3까지 사용하던 양식 유지). 운영 중이며 이 실험과 무관.
> - **`textbook144m.html`** — 본 실험. 교재 양식을 그대로 모바일에 옮기되, 학생앱 인터랙션(섹션 네비/탭 재생/해석 토글)을 입혀 새로운 학생앱으로 통일 가능할지 검증.
>
> 결과가 충분히 좋으면 `app144s.html`을 `textbook144m.html`로 대체하는 통합을 검토. 아닐 경우 교재는 인쇄용으로만 유지.

---

## 결정 사항

### 파일 분리 정책
- 교재(인쇄·B5): `textbook144.html` — 데스크톱 미리보기 + 인쇄 전용. 모바일 미디어쿼리는 fallback 수준만 유지.
- 모바일 앱(실험): `textbook144m.html` — 별도 파일. 교재 디자인 토큰/렌더 함수만 공유, UI 셸은 완전히 다름.
- 기존 학생앱(`app144s.html`)은 손대지 않는다 (운영 중).

### 디자인 토큰 / 시각 언어
교재(`textbook144.html`)와 동일한 토큰을 사용:
- `--bg: #F2F4F6` / `--accent: #1D49FF` / `--hl-text: #3554C6` / `--text: #12205A` / `--sub: #888FAC` / `--border: #D5DAE8` (공통 토큰 — `product-ata144-design.md` 참고)
- 폰트: Noto Sans Arabic + Spoqa Han Sans Neo
- 카드 / 둥근 모서리 / 옅은 그림자 → 교재 카드와 동일

### 데이터 / 렌더 로직
교재의 페이지 단위 렌더 함수(`renderExpressionPage`, `renderPatternPage`, `renderDrillPage`, `renderNassPage`)를 모바일용으로 포팅. 핵심 로직 동일:
- 시트 페치 (`fetchSheet` + CORS / JSONP 폴백)
- 컬럼명 호환 (`note` ↔ `transliteration`)
- drill 탭 fill-down
- 인라인 마크업 (`*...*` 색, `_..._` 박스, `\n` 줄바꿈, `>` 인용)
- 라하 깃발 매핑 (다중 라하 지원)
- baseline 6칸 그리드 + tatweel 빈칸

차이점: 페이지 셸(헤더·푸터)은 모바일용 미니 셸로 교체 (`m-head`, `m-body`, `m-card` 등 클래스 prefix `m-`).

---

## 모바일 앱 UI 구조

```
┌──────────────────────────┐
│ ☰  유닛 A01      1 / 6   │  ← 상단바 48px
├──────────────────────────┤
│ 표현 │ 패턴 │ 드릴 │ 지문 │  ← 섹션 탭 44px
├──────────────────────────┤
│                          │
│   [현재 페이지 콘텐츠]    │  ← 스크롤 영역
│   (교재 디자인 그대로)    │
│                          │
├──────────────────────────┤
│ ◀  01 / 06  ▶  해석 가리기│  ← 하단바 64px
└──────────────────────────┘
```

### 상단바 (48px)
- ☰ 햄버거 (사이드바 열기 — 유닛 픽커)
- 유닛 표시 + 현재 페이지 (예: `유닛 A01  3 / 6`)
- (토글은 하단으로 이동 — 학생앱과 동일 패턴)

### 섹션 탭 (44px)
- 4개 탭: 표현 / 패턴 / 드릴 / 지문
- 활성 탭은 액센트 색 + 하단 라인
- 데이터 없는 섹션은 disabled
- 탭하면 해당 섹션의 첫 페이지로 점프

### 페이지 콘텐츠 (스크롤)
교재 페이지 1장씩 표시. 모바일 폰트/패딩으로 조정.

**카드 1열 스택** (교재의 2열 → 모바일 1열):
- 표현 카드: 아랍어 위 → 음가 → 한국어 (밑줄 없는 단순 스택)
- 나쓰 카드: 아랍어 위 → 음가 → 한국어 → 깃발

**baseline (알파벳 그룹)**: 6칸 비례 그대로 (1/6 폭). 글자 크기는 모바일 폭에 맞춰 축소.

**드릴 5열 표**: 모바일(≤480px)에선 자동으로 **4열로 전환** (`window.matchMedia` 기반 런타임 분기). 좁은 폭에서 5열 가독성 문제 회피.

**한국어 / 음가 폰트 크기**: 일반 모바일 표준 (12-13px = 0.78-0.82rem) — 데스크톱보다 훨씬 작게.

### 하단바 (64px)
- 좌측: ◀ prev (첫 페이지에서 disabled)
- 중앙: 페이지 인디케이터 (`02 / 06`)
- 그 다음: ▶ next (마지막 페이지에서 disabled)
- 우측: **해석 가리기 / 보기** 단일 토글 버튼
  - 한국어 + 음가를 동시에 가림 (한 번에 둘 다)
  - 텍스트 변환: 보임 상태 = "해석 가리기", 가림 상태 = "해석 보기"
  - 상태는 `localStorage('m.hide-meaning')`에 영구 저장 — 앱 재진입 시 유지
  - 학생앱 `app144s.html`의 `meaning-btn` 패턴 차용

### 사이드바 (유닛 픽커)
- 좌측에서 슬라이드 아웃, 마스크 클릭 닫기
- 유닛 리스트 (`u` 값 + 한국어 라벨 일부)
- 현재 유닛은 액센트 강조

---

## 인터랙션

### 탭 → 오디오 재생
모든 아랍어 텍스트 요소에 `.tap-audio` 클래스 + `data-audio` 속성 부여:
- 탭하면 GCS의 오디오(`listening(144)/{id}.mp3`) 재생
- 재생 중 해당 요소만 액센트 색(`var(--accent)`)으로 하이라이트 (`.playing` 클래스)
- 같은 항목 재탭 → 정지
- 다른 항목 탭 → 이전 정지 후 새로 재생

URL 우선순위:
1. 시트 `url` 컬럼
2. `listening(144)/{id_drill or id_kalimat or id_nass or id_ptn}.mp3` 자동 생성

오디오 파일 / id가 없으면 클릭해도 무동작 (콘솔 경고).

### 페이지 네비게이션
- 하단 ◀ ▶ 버튼
- 섹션 탭으로 점프
- (Phase 2) 좌우 스와이프 제스처 — 미구현, 추후

### 해석 토글
- 학생앱과 동일하게 단일 버튼
- 한국어와 음가를 함께 토글 (`.hide-ko` + `.hide-trans` 동시 적용)
- 영구 상태 (localStorage)

---

## 알려진 이슈 / 추후 작업

- [ ] **좌우 스와이프 제스처** (Phase 2) — touchstart/touchmove로 swipe 감지
- [ ] **진도 표시** — 읽은 페이지/유닛 표시 (localStorage)
- [ ] **북마크** — 즐겨찾기 페이지
- [ ] **검색** — 표현/지문 검색
- [ ] **오디오 자동재생 큐** — 페이지의 모든 오디오를 순차 재생
- [ ] **다크 모드** — 디자인 토큰 다크 변형
- [ ] **다운로드/오프라인** — PWA 가능성

## 학생앱과의 통합 검토 시점

다음 조건이 만족되면 `app144s.html` → `textbook144m.html` 통합 검토:
1. 모든 페이지 종류(표현/패턴/드릴/지문)에서 인터랙션이 자연스러움
2. 모바일 가독성이 학생앱보다 좋거나 동등
3. 오디오 재생 흐름이 슬라이드형(현재 학생앱)보다 직관적
4. 학습 보조 기능(진도/북마크) 구현 완료

통합 결정 시:
- `app144s.html`을 백업하고 `textbook144m.html`을 새 학생앱으로 승격
- URL/배포 경로 갱신
- 학생 사용자 안내

통합 보류 시:
- 본 실험을 종료하고 `textbook144m.html`은 사내 시연/검토용으로만 유지
- 교재(`textbook144.html`)는 인쇄용으로 정상 운영

---

## 파일 인벤토리

| 파일 (현재 경로) | 역할 | 상태 |
|---|---|---|
| `ata144_student/ata144_student.html` | 운영 중인 학생앱 (1.4.3 양식 기반) | **운영 중**, 손대지 않음 |
| `ata144_original/ata144_original.html` | 인쇄용 교재 (B5) | 운영 중 |
| `ata144_textbook/ata144_textbook.html` | 모바일 앱 실험 (교재 양식 기반) | **실험 중** |
| `product-ata144.md` | 제품 종합 문서 (앱 + 교재) | 운영 중 |
| `product-ata144-design(mobile).md` | 본 문서 — 모바일 실험 기록 | (이 파일) |
